# Particles/Sound/Game-Feel + AI/Combat analysis — 12 games

## Guarding Athena (Dota 2 custom game, id 530286038)
  PARTICLES:
    - Custom projectile system creates + binds the .vpcf itself (PATTACH_CUSTOMORIGIN): ProjectileSystem:CreateLinearProjectile/CreateTracking/CreateGuidance each spawn their own particle with PATTACH_CUSTOMORIGIN (no parent ent), then drive it every FrameTime via control points. CP0 = position, CP1 = velocity, plus SetParticleControlForward(0, dir) so the FX faces travel direction. Tracking projectiles instead bind CP1 to the target via SetParticleControlEnt(...PATTACH_POINT_FOLLOW,'attach_hitloc'). The particle index is stored in tInfo.iParticleID so the system can DestroyParticle on hit/expire. An optional sEffectName lets callers skip auto-creation and bind a hand-made particle to iParticleID. Known-bug note in the header: bouncing recreates the FX (one-frame disappear + trail reset). (scripts/vscripts/mechanics/projectile_system.lua)
      local iParticleID = ParticleManager:CreateParticle(tInfo.sEffectName, PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControlForward(iParticleID, 0, tInfo.vVelocity:Normalized())
      ParticleManager:SetParticleControl(iParticleID, 0, tInfo.vSpawnOrigin)
      ParticleManager:SetParticleControl(iParticleID, 1, tInfo.vVelocity)
      tInfo.iParticleID = iParticleID
    - One-shot FX use ReleaseParticleIndex immediately after setting CPs: The pervasive cleanup idiom: create, set control points, then ReleaseParticleIndex right away. The particle keeps playing to completion but the engine reclaims the index so it auto-frees with no Lua bookkeeping. Used for lifesteal pings, cleave, and the floating damage-number FX. modifier_common's cleave also chains targets onto sequential CPs (CP n+1 per extra unit) and packs count metadata into a CP vector. (scripts/vscripts/modifiers/modifier_common.lua)
      local iParticleID = ParticleManager:CreateParticle("particles/generic_gameplay/generic_lifesteal.vpcf", PATTACH_ABSORIGIN_FOLLOW, hParent)
      ParticleManager:ReleaseParticleIndex(iParticleID)
    - Floating crit damage numbers via msg_crit.vpcf control-point encoding: Damage numbers are a particle, not a UI element. On an incoming-damage crit they create msg_crit.vpcf attached PATTACH_OVERHEAD_FOLLOW, then encode the number into control points: CP1=(0, value, digit-style), CP2=(duration, #digits+1, 0), CP3=color (cyan for magical crit, red for physical). Then ReleaseParticleIndex. This is the standard Valor/'message FX' floating-text trick. (scripts/vscripts/modifiers/modifier_common.lua)
      local iParticleID = ParticleManager:CreateParticle("particles/msg_fx/msg_crit.vpcf", PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControlEnt(iParticleID, 0, hTarget, PATTACH_OVERHEAD_FOLLOW, nil, hTarget:GetAbsOrigin(), true)
      ParticleManager:SetParticleControl(iParticleID, 1, Vector(0, iNumber, bMagicalCrit and 6 or 4))
      ParticleManager:SetParticleControl(iParticleID, 2, Vector(fDuration, #sNumber + 1, 0))
      ParticleManager:SetParticleControl(iParticleID, 3, vColor)
      ParticleManager:ReleaseParticleIndex(iParticleID)
    - Modifier-owned persistent FX created client-side + AddParticle for auto-cleanup: Looping/aura FX are created inside IsClient() guards (saves a server->client particle replication) and registered with self:AddParticle(id, ...) so the engine destroys them automatically when the modifier ends. Multiple attach points are wired with SetParticleControlEnt + PATTACH_POINT_FOLLOW to model attachment names (attach_weapon, attach_wing_l/r). boss_hell_fist's debuff does the same with two stacked doom_infernal_blade particles. (scripts/vscripts/modifiers/courier_fx/modifier_courier_fx_ambient_9.lua)
      if IsClient() then
        local iParticleID = ParticleManager:CreateParticle("particles/econ/courier/.../courier_golden_doomling_ambient.vpcf", PATTACH_ABSORIGIN, self:GetParent())
        ParticleManager:SetParticleControlEnt(iParticleID, 0, self:GetParent(), PATTACH_POINT_FOLLOW, "attach_weapon", self:GetParent():GetAbsOrigin(), true)
        self:AddParticle(iParticleID, true, false, -1, false, false)
      end
    - Reusable chain-lightning FX helper (LightningStrike) hops particle+sound between targets: CDOTA_BaseNPC:LightningStrike is a custom method that draws a particle from source attach point to each successive target's attach_hitloc, re-finds the next target with FindUnitsInRadius(FIND_CLOSEST) after flJumpDelay, and replays EmitSoundOnLocationWithCaster at each hop. Passing the callback as a string path auto-creates+releases the bolt FX; passing a function lets the caller draw it. (scripts/vscripts/abilities/init.lua)
      local iParticleID = ParticleManager:CreateParticle(callback, PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControlEnt(iParticleID, 0, _hSource, PATTACH_POINT_FOLLOW, bFirst and sAttachName or "attach_hitloc", _hSource:GetAbsOrigin(), false)
      ParticleManager:SetParticleControlEnt(iParticleID, 1, _hTarget, PATTACH_POINT_FOLLOW, "attach_hitloc", _hTarget:GetAbsOrigin(), false)
      ParticleManager:ReleaseParticleIndex(iParticleID)
    - Panorama-side particles: cast-range ring + DOTAParticleScenePanel tooltips: The HUD ability JS creates a client particle (range_display.vpcf, PATTACH_CUSTOMORIGIN) and binds CP0 to the hero entity and CP1 to cast range to draw the cast-range ring while hovering an ability. Aghanim status tooltips render a particle-only scene inside <DOTAParticleScenePanel particleonly cameraOrigin lookAt fov hittest> instead of a static image. (panorama/scripts/custom_game/ability.js)
      m_iCastRangeParticleID = Particles.CreateParticle("particles/ui_mouseactions/range_display.vpcf", ParticleAttachment_t.PATTACH_CUSTOMORIGIN, -1);
      Particles.SetParticleControlEnt(m_iCastRangeParticleID, 0, m_iEntityIndex, ParticleAttachment_t.PATTACH_ABSORIGIN_FOLLOW, undefined, Entities.GetAbsOrigin(m_iEntityIndex), true);
      Particles.SetParticleControl(m_iCastRangeParticleID, 1, [fCastRange, 0, 0]);
  SOUNDS:
    - Reusable location sound with caster (EmitSoundOnLocationWithCaster): Ability impacts/chains play through EmitSoundOnLocationWithCaster(vLoc, sSoundName, hCaster) so positional sound follows the hit point rather than the unit. Used by the generic LightningStrike helper and the projectile/AOE helpers in init.lua, replaying per jump. (scripts/vscripts/abilities/init.lua)
    - Channel sound paired with StopSound on release (Powershot): windrunner_1 plays Hero_Windrunner.Powershot.Channel on OnAbilityPhaseStart, explicitly StopSound's it on OnChannelFinish, then fires Ability.Powershot when the arrows actually launch — classic charge/release audio shaping. EmitSound calls are on the unit handle (hCaster:EmitSound). (scripts/vscripts/abilities/heroes/windrunner/windrunner_1.lua)
    - Borrowed Valve hero soundevents reused verbatim across custom enemies: Rather than authoring .vsndevts, the game maps existing Dota soundevent names onto custom abilities: ElderTitan.EarthSplitter (earthshake), SandKing_Epicenter (boss_epicenter), Tidehunter.AnchorSmash, Techies.Suicide.Arcana (bomb_suicide), LifeStealer.Infest/Consume (alien_parasitism). heroeskv.js still declares per-hero GameSoundsFile/VoiceFile .vsndevts paths for picked heroes. (scripts/vscripts/abilities/enemy/earthshake.lua)
    - Global gameplay-beat sound on wave spawn (EmitGlobalSound): The wave spawner fires EmitGlobalSound("Tutorial.Quest.complete_01") at the start of each spawn batch as a non-positional 'wave incoming' cue to all players. (scripts/vscripts/class/spawner.lua)
    - Panorama UI feedback sounds via Game.EmitSound: UI panels use Game.EmitSound for affordance: team_select plays ui_team_select_pick_team / ..._failed on pick success/failure; the chat/notification panel plays Chat.Team.Received, ui_chat_msg_send, ui_chat_msg_rec. (panorama/scripts/custom_game/team_select.js)
    - Crit-driven attack sound override via MODIFIER_PROPERTY_TRANSLATE_ATTACK_SOUND: modifier_common declares TRANSLATE_ATTACK_SOUND and GetAttackSound returns a critical-strike sound only when the damage record is flagged as a physical crit (DamageFilter ... PHYSICAL_CRIT), so crits get a distinct impact sound through the engine's attack-sound translation hook. (scripts/vscripts/modifiers/modifier_common.lua)
  GAME-FEEL:
    - Knockback for impact + on-death scatter: Knockback is the main motion juice. Powershot pushes hit units back along the shot vector scaled by distance-to-attacker; split-on-death enemies fling their summoned children outward with RandomVector so a popped unit visibly bursts apart.
      hTarget:KnockBack((hTarget:GetAbsOrigin() - vStart):Normalized(), _flDistance, 0, 0.3)
      -- on death:
      hUnit:KnockBack(RandomVector(1), 300, 200, 1)
    - Floating damage numbers (particle-based combat text): Crit hits spawn msg_crit.vpcf overhead with the damage value, digit count, duration and color packed into control points — cyan for magical crits, red for physical — giving readable, color-coded damage feedback without any Panorama widget.
      local vColor = bMagicalCrit and Vector(0, 191, 255) or Vector(255, 32, 32)
      ParticleManager:SetParticleControl(iParticleID, 1, Vector(0, iNumber, bMagicalCrit and 6 or 4))
      ParticleManager:SetParticleControl(iParticleID, 2, Vector(fDuration, #sNumber + 1, 0))
    - Charge-and-release ability shaping (channel time -> distance/spread/damage): Powershot RemapVal's the actual channel time into projectile distance, fan angle (15deg->5deg as it charges), bonus damage, and even cooldown reduction — the longer you hold, the tighter and harder the shot, with gesture + release sound on fire. This gives weighty, skill-expressive feel.
      local flDistance = RemapVal(flChannelTime, 0, self:GetChannelTime(), 0, self:GetSpecialValueFor("bonus_range")) + self:GetSpecialValueFor("range")
      local flAngle = RemapVal(flChannelTime, 0, self:GetChannelTime(), 15, 5)
      hCaster:StartGesture(ACT_DOTA_OVERRIDE_ABILITY_2)
    - Camera lock-on via PlayerResource:SetCameraTarget: Demo/debug and spawn tooling snap the player camera to a unit with PlayerResource:SetCameraTarget(playerID, unit) and release it with nil — used both for the debug 'draw circle' tool and for focusing spawned/controlled units, the building block for any cinematic camera focus.
      PlayerResource:SetCameraTarget(tData.PlayerID, tData.Unit)
      -- release:
      PlayerResource:SetCameraTarget(tData.PlayerID, nil)
    - Boss enrage ramp instead of hitstop/timescale: No literal hit-stop/time-scale is used; boss tension comes from a time-based incoming-damage ramp. modifier_boss_buff returns RemapVal(elapsed, 0, duration, -damage_reduce, 0) so when a boss crosses an HP threshold it becomes briefly near-immune and the value decays back to 0 — a readable 'phase shield' beat.
      function modifier_boss_buff:GetModifierIncomingDamage_Percentage(params)
        return RemapVal(self:GetElapsedTime(), 0, self:GetDuration(), -self.damage_reduce, 0)
      end
    - Cast gestures + AOE pulse FX scaled by radius for telegraphing: Boss abilities StartGesture the matching cast animation and override animation via MODIFIER_PROPERTY_OVERRIDE_ANIMATION, then pulse sandking_epicenter.vpcf with CP1 = Vector(radius,radius,radius) each tick so the ground shockwave visually grows to its real hit radius — telegraphing the danger zone.
      hCaster:StartGesture(ACT_DOTA_CAST_ABILITY_4)
      ParticleManager:SetParticleControl(iParticleID, 1, Vector(self.radius, self.radius, self.radius))
  AI/COMBAT:
    - ability_base_ai — generic auto-cast ability AI framework: Base class every enemy/boss ability inherits. On Spawn it parses the kv behavior (NO_TARGET/POINT/UNIT_TARGET) and starts a GameTimer think loop at AI_TIMER_TICK_TIME. When the ability is ready it FindUnitsInRadius within cast range using kv target team/type/flags, optionally sorts via funcSortFunction (e.g. lowest mana/HP) or filters via funcCondition, then ExecuteOrder-casts. Search behaviors include AI_SEARCH_BEHAVIOR_MOST_AOE_TARGET (GetAOEMostTargetsPosition) and MOST_LINE_TARGET (GetLinearMostTargetsPosition) for smart point-spell placement. (scripts/vscripts/abilities/ability_base_ai.lua)
    - Boss/Captain phase-threshold enrage AI: modifier_boss / modifier_captain compute HP thresholds = MaxHealth*(1-damage_limit%). MIN_HEALTH clamps the boss at the next threshold so it can't be burst past it; crossing a threshold (OnTakeDamage when health==threshold) grants a temporary stacking incoming-damage-reduction buff and steps the threshold down on an interval — i.e. a multi-stage damage-gated phase fight. Captain adds movespeed/attackspeed/crit and 50x model scale for elite presence. (scripts/vscripts/abilities/enemy/boss.lua)
    - Wave/champion/elite spawner (Holdout-style director): CSpawner Think loop spawns UnitsPerSpawn every SpawnInterval up to TotalUnitsToSpawn, with WaitForUnit/GroupWithUnit dependency chaining between spawners. RollPercentage(ChampionChance) promotes units to champions (CreatureLevelUp + ChampionBonusLevel + modifier_champion + bigger model); on high difficulty wave_ units get modifier_elite by chance. Each enemy SetGoalEntity(spawner) and is tagged iDefendingTeamNumber to drive lane pathing toward the base. (scripts/vscripts/class/spawner.lua)
    - Custom projectile patterns: linear fan, tracking, guidance, surround/orbit: ProjectileSystem supplies LINEAR, TRACKING, GUIDANCE (angular-velocity homing with auto-retarget via flTrackRadius) and SURROUND (orbiting groups that auto-space N projectiles 360/N) movement types, plus SplitAction to fan a shot into N directions over a clamped total angle, CounterProjectile to reflect, and per-team speed multipliers. Powershot uses it to launch a 5-arrow RotatePosition fan. (scripts/vscripts/mechanics/projectile_system.lua)
    - AOE pulse boss ability (Epicenter) — interval FindUnitsInRadius damage: boss_epicenter overrides GetRadius from kv so ability_base_ai targets it; on cast it StartGesture + AddNewModifier that StartIntervalThink pulses every 0.5s, FindUnitsInRadiusWithAbility each tick to DealDamage and apply a -50% move/attack-speed debuff, redrawing the shockwave FX sized to radius. A clean template for telegraphed boss ground-AOE phases. (scripts/vscripts/abilities/nature/boss_epicenter.lua)
    - Split-on-death enemy AI (slime/golem): alien_split / golem_spilt are intrinsic modifiers listening to MODIFIER_EVENT_ON_DEATH; on death they SummonUnit N smaller copies at the death origin (enigma -> small/split variants) and KnockBack each child with RandomVector so they scatter — a self-propagating add-spawning combat behavior that pressures area control. (scripts/vscripts/abilities/enemy/alien_split.lua)

## Dota IMBA (Workshop id 440115357)
  PARTICLES:

  SOUNDS:
    - Looping sound paired 1:1 with looping particle (EmitSoundOn on create, StopSound on destroy): Looping/channel sounds are emitted on a unit on modifier create and explicitly stopped on destroy/finish so they don't hang. Examples: Alchemist Acid Spray thinker:StopSound('Hero_Alchemist.AcidSpray'), Concoction fuse caster:StopSound('Hero_Alchemist.UnstableConcoction.Fuse'), Arc Warden StopSound('Hero_ArcWarden.MagneticField'/'SparkWraith.Loop'), Bane StopSoundOn('Hero_Bane.FiendsGrip.Cast', fiendcaster). The loop sound lifecycle mirrors the particle lifecycle. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/heroes/hero_kunkka.lua)
    - EmitSoundOnLocationWithCaster for positional, caster-attributed casts: For abilities that should sound at a world point but be attributed to the caster (correct stereo/announcer ownership), they use EmitSoundOnLocationWithCaster(location, 'Ability.Torrent'/'Hero_ArcWarden.SparkWraith.Appear'/'Hero_Ancient_Apparition.IceBlast.Target', caster). Used heavily for delayed/landing AoE so the sound plays at impact, not on the caster. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/heroes/hero_arc_warden.lua)
    - Custom soundevents banks (imba_soundevents.vsndevts / imba_item_soundevents.vsndevts) + custom event names: The mod ships its own soundevent banks precached in addon_game_mode.lua and references custom events like 'Imba.AbaddonHeyYou' (stopped via StopSoundEvent(name, parent)). Vanilla hero sound events (Hero_Kunkka.Tidebringer.Attack, Hero_Antimage.ManaVoid) are reused alongside the custom 'Imba.*' bank. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/heroes/hero_abaddon.lua)
    - Panorama Game.EmitSound for kill-feed feedback (self/teammate kill & death): The custom combat-log toast plays distinct UI sounds based on the local player's role in the event: Game.EmitSound('notification.self.kill') when the local player gets the kill, 'notification.self.death' when they die, and 'notification.teammate.kill' for ally kills. Pure client-side audio reaction to a custom net event, giving each player personalized feedback. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/panorama/scripts/custom_game/combat_events.js)
    - Announcer countdown via dynamically-built sound event name: Hero selection plays the pick-phase countdown by concatenating the remaining seconds into the announcer event name: Game.EmitSound('announcer_ann_custom_countdown_' + data['time']) (with a zero-padded variant for single digits). Other UI sounds: 'ui.crafting_gem_create' (chat wheel), 'inventory.wear' (hero demo), 'General.Buy'/'General.Cancel'/'ui_rollover_micro' (item builds). (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/panorama/scripts/custom_game/hero_selection.js)
  GAME-FEEL:
    - ScreenShake on heavy nuke impact: Antimage's empowered Mana Void shakes the screen at the target's location for emphasis. Signature is ScreenShake(center, amplitude, frequency, duration, radius, command, bAirShake) — here amplitude 10, frequency 0.1, duration 1s, radius 500. This is the only ScreenShake call in the dumped ability set, reserved for the game's biggest burst-damage moment.
      -- Shake screen due to excessive PURITY OF WILL
      ScreenShake(target:GetOrigin(), 10, 0.1, 1, 500, 0, true)
    - Floating damage numbers via SendOverheadEventMessage: Bonus/special damage and mana burn are surfaced as floating overhead numbers with SendOverheadEventMessage(player, OVERHEAD_ALERT_BONUS_SPELL_DAMAGE | OVERHEAD_ALERT_MANA_LOSS, target, amount, nil). Passing nil as the player makes it visible to everyone. Used pervasively (Ancient Apparition, Antimage) so players see exact proc damage/mana-burn pop on the victim.
      SendOverheadEventMessage(nil, OVERHEAD_ALERT_BONUS_SPELL_DAMAGE, enemy, damage, nil)
      SendOverheadEventMessage(nil, OVERHEAD_ALERT_MANA_LOSS, target, target_mana_burn, nil)
    - Knockback via reusable modifier_knockback + dummy as motion anchor: The Frostbitten/force tower throws enemies using AddNewModifier(caster, nil, 'modifier_knockback', params) with center_x/y/z, knockback_distance/duration/height and should_stun. For heroes they spawn a temporary npc_dummy_unit offset along the push direction, set its position as the knockback center (so the push aims outward), then Destroy() the dummy after reading its origin. RemoveModifierByName('modifier_knockback') first prevents stacking-glitch motion.
      knockback_param = { should_stun = 1, knockback_duration = force_duration, duration = force_duration,
        knockback_distance = force_distance, knockback_height = 0,
        center_x = tower_loc.x, center_y = tower_loc.y, center_z = tower_loc.z }
      enemy:RemoveModifierByName("modifier_knockback")
      enemy:AddNewModifier(caster, nil, "modifier_knockback", knockback_param)
    - Layered AoE 'juice' via timed polar mini-effects: Tidebringer-tsunami Torrent doesn't fire one ring; it floods the radius with staggered mini-splashes. A loop over count_mini = floor(radius/35) places each splash at angle (360/count_mini)*i with randomized radius/timing via Timers:CreateTimer(math.random(80)*0.01, ...). The randomized delays + positions read as a chaotic, organic water eruption instead of a single sterile particle.
      for i = 0, count_mini, 1 do
        Timers:CreateTimer(math.random(80) * 0.01, function()
          local angle = (360 / count_mini) * i
          local mini_target = target + Vector(math.cos(angle)*border, math.sin(angle)*border, 0)
          ... CreateParticle at mini_target ... end) end
    - Client-side kill-feed flair (CSS classes + sound keyed to local player): Combat toast rows get role classes toggled in JS — AllyEvent/EnemyEvent/LocalPlayerInvolved/LocalPlayerKiller/LocalPlayerVictim — so CSS can color/animate the feed differently when the local player is the killer vs victim, and the matching EmitSound is fired. This couples visual emphasis and audio to whether the moment matters to YOU.
      row.SetHasClass('LocalPlayerKiller', isKiller);
      row.SetHasClass('LocalPlayerVictim', isVictim);
      if (isKiller) Game.EmitSound('notification.self.kill');
      else if (isVictim) Game.EmitSound('notification.self.death');
  AI/COMBAT:
    - Tower auto-cast targeting via FindUnitsInRadius gates (min-creeps / hero-present): Tower spell-cast logic (hex/burn/stun/force in tower_abilities.lua) runs FindUnitsInRadius twice per cast — once for DOTA_UNIT_TARGET_BASIC creeps and once for DOTA_UNIT_TARGET_HERO — and only fires when '#creeps >= min_creeps or #heroes >= 1', then EmitSound, applies the effect to every found unit, and StartCooldown(GetCooldown(-1)). This threshold-gated FindUnitsInRadius pattern is the core tower AI decision. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/buildings/tower_abilities.lua)
    - Multishot tower: cleave to nearest extra targets on attack: modifier_imba_tower_multishot reacts to MODIFIER_EVENT_ON_ATTACK: on its parent's attack it FindUnitsInRadius(... attack range, DOTA_UNIT_TARGET_CREEP, FIND_CLOSEST ...), iterates skipping the original target, and PerformAttack(enemy, true,true,true,false,true,false,false) on up to arrow_count units that HasAttackCapability. Guards against PassivesDisabled/no_attack_cooldown to avoid recursion. Classic tower-defense split-shot AI. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/buildings/towers/multishot.lua)
    - Concentrated Momentum tower: ramping attack speed for focusing one target: Rewards a tower for kiting/focusing: on MODIFIER_EVENT_ON_ATTACK_START it compares params.target to modifier.last_target; same target -> IncrementStackCount up to max_stacks, new target -> reset to 1. The stack modifier grants GetModifierAttackSpeedBonus_Constant = bonus_as * stacks. Encodes 'lock onto one creep to spin up DPS' behavior with stateful per-target tracking. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/buildings/towers/concentrated_momentum.lua)
    - Mars Arena of Blood: projectile-interception aura via tracking-projectile filter + interval poll: The arena modifier acts as combat AI for incoming attacks: ProjectileFilter(data) lets allied/self/non-attack projectiles through (return true) but for enemy attack projectiles where attacker OR target has the arena modifier it spawns a NEW CreateTrackingProjectile copy (using a self.lock guard to avoid filtering its own re-fire) and registers the id. OnIntervalThink (StartIntervalThink 0.03) polls GetTrackingProjectileLocation for every tracked id and, when |distance-radius| < width, DestroyTrackingProjectile + plays wall-impact effects — i.e. the wall physically blocks projectiles crossing it. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/heroes/hero_mars.lua)
    - Linear projectile spec (Mirana Arrow) with vision, expiry, and ExtraData payload: ProjectileManager:CreateLinearProjectile with a full table: vSpawnOrigin, fDistance, fStart/EndRadius, vVelocity = direction*(speed+talentbonus)*Vector(1,1,0), bDeleteOnHit, iUnitTargetTeam/Type, fExpireTime = GameRules:GetGameTime()+10, and bProvidesVision/iVisionRadius. ExtraData carries the cast origin (cast_loc_x/y/z as strings) which OnProjectileHit_ExtraData reads to scale stun by travel distance. Shows the standard 'pack context into ExtraData' projectile-AI idiom. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/heroes/hero_mirana.lua)
    - Tracking projectile as a redirected/auto-attack (Mars), with iMoveSpeed + bIsAttack: Mars re-creates intercepted attacks as homing projectiles: CreateTrackingProjectile{ Target, Source, Ability, EffectName = attacker:GetRangedProjectileName(), iMoveSpeed = data.move_speed, bDodgeable = true, vSourceLoc = attacker:GetAbsOrigin(), bIsAttack = true, ExtraData = data }. Using bIsAttack=true preserves attack semantics (procs/on-hit), and the returned id is stored so the interval loop above can manage it. Demonstrates programmatic homing-attack generation. (C:/Users/work/.dota2-workshop-mcp/reflib/items/440115357/files/scripts/vscripts/components/abilities/heroes/hero_mars.lua)

## Dota 2 Horde Mode (id 472597026)
  PARTICLES:
    - Fire-and-forget particle with dual hitloc attachment (ReleaseParticleIndex): Canonical transient-FX pattern: CreateParticle, bind BOTH control points to entities via SetParticleControlEnt with the 'attach_hitloc' attach point (so the beam connects caster and target hit locations), then ReleaseParticleIndex so the engine auto-cleans the effect when it finishes. Used for instant beams/links that don't need to be held. (scripts/vscripts/heroes/hero_bane/brain_sap_lua.lua)
      local pfx = ParticleManager:CreateParticle("particles/units/heroes/hero_bane/bane_sap.vpcf", PATTACH_CUSTOMORIGIN_FOLLOW, unit)
      ParticleManager:SetParticleControlEnt(pfx, 0, caster, PATTACH_ABSORIGIN_FOLLOW, "attach_hitloc", caster:GetAbsOrigin(), true)
      ParticleManager:SetParticleControlEnt(pfx, 1, unit, PATTACH_ABSORIGIN_FOLLOW, "attach_hitloc", unit:GetAbsOrigin(), true)
      ParticleManager:ReleaseParticleIndex(pfx)
    - Persistent particle stored on entity + manual DestroyParticle on modifier end: For looping/held buffs the particle index is cached as a field on the unit (target.ShieldParticle), attached with PATTACH_POINT_FOLLOW + 'attach_hitloc' (only POINT_FOLLOW gives correct shield position). It is explicitly DestroyParticle'd in the modifier's removal handler. Note the 0.01s Timer wait so the previous frame's particle is destroyed before recreating. (scripts/vscripts/heroes/hero_abaddon/aphotic_shield.lua)
      Timers:CreateTimer(0.01, function()
        target.ShieldParticle = ParticleManager:CreateParticle("particles/units/heroes/hero_abaddon/abaddon_aphotic_shield.vpcf", PATTACH_ABSORIGIN_FOLLOW, target)
        ParticleManager:SetParticleControlEnt(target.ShieldParticle, 0, target, PATTACH_POINT_FOLLOW, "attach_hitloc", target:GetAbsOrigin(), true)
      end)
      -- ...later, on modifier destroy:
      ParticleManager:DestroyParticle(target.ShieldParticle,false)
    - Modifier-owned particle lifecycle via self:AddParticle(): Inside a Lua modifier, particles are registered with self:AddParticle(idx, ...) so the engine ties them to the modifier's lifetime and auto-destroys them when the modifier ends (no manual bookkeeping). Glimpse spawns three layered particles (travel/targetend/targetstart) per glimpsed unit, each driven by control points 1 (destination) and 2 (duration vector). (scripts/vscripts/heroes/hero_disruptor/modifier_disruptor_glimpse_thinker_nb2017.lua)
      local nFXIndex = ParticleManager:CreateParticle( "particles/units/heroes/hero_disruptor/disruptor_glimpse_travel.vpcf", PATTACH_CUSTOMORIGIN, nil )
      ParticleManager:SetParticleControlEnt( nFXIndex, 0, hUnit, PATTACH_ABSORIGIN_FOLLOW, nil, hUnit:GetOrigin(), true )
      ParticleManager:SetParticleControl( nFXIndex, 1, vOldLocation )
      ParticleManager:SetParticleControl( nFXIndex, 2, Vector( flDuration, flDuration, flDuration ) )
      self:AddParticle( nFXIndex, false, false, -1, false, false )
    - Floating-text via msg_fx particles (DotaCraft popups library): Reusable damage/heal/gold numbers system. Loads particles/msg_fx/msg_<type>.vpcf and packs data into control points: CP1 = (preSymbol, number, postSymbol), CP2 = (lifetime, digitCount, 0), CP3 = RGB color. Team-colored variants (gold/lumber) use CreateParticleForTeam so only the owning team sees them; everything else uses plain CreateParticle with PATTACH_ABSORIGIN_FOLLOW. (scripts/vscripts/libraries/popups.lua)
      pidx = ParticleManager:CreateParticleForTeam(pfxPath, PATTACH_ABSORIGIN_FOLLOW, target, target:GetTeamNumber())
      -- ...
      ParticleManager:SetParticleControl(pidx, 1, Vector(tonumber(presymbol), tonumber(number), tonumber(postsymbol)))
      ParticleManager:SetParticleControl(pidx, 2, Vector(lifetime, digits, 0))
      ParticleManager:SetParticleControl(pidx, 3, color)
    - Thinker-driven particle swap + projectile spawn (spark wraith): A modifier thinker manages a multi-stage VFX state machine: create a 'startup' sphere particle, DestroyParticle it when it activates and create the 'active' sphere, then on enemy detection destroy the active particle and fire a tracking projectile. OnDestroy guards by destroying any live particle. Shows full create->destroy->recreate->release discipline keyed off think states. (scripts/vscripts/heroes/hero_arc_warden/arc_warden_spark_wraith_lua.lua)
      ParticleManager:DestroyParticle(self.startup_particle, false)
      self.particle = ParticleManager:CreateParticle("particles/units/heroes/hero_disruptor/disruptor_thunder_strike_buff_sphere.vpcf", PATTACH_WORLDORIGIN, thinker)
      ParticleManager:SetParticleControl(self.particle, 3, (thinker_pos + Vector(0, 0, 150)))
    - Particle-folder precache in addon entry point: Precaching is done once in addon_game_mode.lua's Precache(context) using PrecacheResource. The shipping game precaches a whole particle folder ('particles/frostivus_gameplay') rather than individual systems, and notes that precaching a parent particle typically precaches its children. Models/sounds use the same context-based API (model/model_folder/soundfile). (scripts/vscripts/addon_game_mode.lua)
      PrecacheResource( "particle_folder", "particles/frostivus_gameplay", context )
      PrecacheItemByNameSync("item_resonant_shard", context)
  SOUNDS:
    - Paired caster + target EmitSoundOn: Spells emit two distinct sound events on cast: one anchored to the caster and one to the target, giving directional/positional audio at both ends of the effect. The standard verb is EmitSoundOn(eventName, entity). Example: EmitSoundOn('Hero_Bane.BrainSap', caster) and EmitSoundOn('Hero_Bane.BrainSap.Target', target). (scripts/vscripts/heroes/hero_bane/brain_sap_lua.lua)
    - Layered looping sound with explicit StopSound on cleanup (chaos meteor): Chaos Meteor stacks a one-shot Cast plus a looping Loop on the caster, then on land transfers a Loop+Impact onto the moving dummy unit, and damage ticks emit a per-hit Damage event. Every loop is paired with a matching StopSound when the dummy/meteor is removed (caster:StopSound('Hero_Invoker.ChaosMeteor.Loop')), preventing orphaned audio. The model for sustained-effect sound hygiene. (scripts/vscripts/heroes/hero_invoker/chaos_meteor.lua)
    - Location/ally-scoped emit + global StopSoundEvent (disruptor glimpse): Uses EmitSoundOnLocationForAllies(pos, event, caster) to play a positional sound only for the friendly team, StopSoundOn(event, unit) to kill a per-unit travel loop when a unit's glimpse ends, and StopSoundEvent(event, nil) globally when the thinker self-destructs. Demonstrates the three scoping variants (location/ally, per-entity, global). (scripts/vscripts/heroes/hero_disruptor/modifier_disruptor_glimpse_thinker_nb2017.lua)
    - EmitSound on modifier-destroy hook: Aphotic Shield plays its break SFX (target:EmitSound('Hero_Abaddon.AphoticShield.Destroy')) inside the same handler that destroys the shield particle, tying the audio cue to the visual end-state of the buff. (scripts/vscripts/heroes/hero_abaddon/aphotic_shield.lua)
    - Ward sound stopped on entity removal (StopSoundEvent): Death Ward stores the spawned ward entity and, on destroy, calls UTIL_Remove(ward) followed by StopSoundEvent(keys.sound, caster) so the ward's looping ambient stops the instant the unit is gone. Sound name is passed in via the data-driven keys table. (scripts/vscripts/heroes/hero_witch_doctor/death_ward.lua)
    - Panorama UI button sounds via CSS 'sound' property: Team-select buttons trigger UI audio purely from CSS pseudo-classes: a button's :active rule sets sound: "<soundevent>". No JS wiring needed; the panel engine plays the named ui soundevent on the interaction state. Events used include ui_team_select_shuffle, ui_team_select_auto_assign, ui_team_select_lock_and_start, ui_team_select_cancel_and_lock. (panorama/styles/custom_game/team_select.css)
  GAME-FEEL:
    - Camera distance override (global zoom-out): In scripts/vscripts/internal/gamemode.lua the gamemode sets a fixed camera distance for the whole mode via the game-mode entity, a classic horde/arena tweak to widen the view. No per-event ScreenShake or SetTimeScale hit-stop exists in this codebase; game-feel is carried by particle popups, camera distance, and motion controllers.
      mode:SetCameraDistanceOverride( CAMERA_DISTANCE_OVERRIDE )
    - Floating damage/heal/gold/crit numbers (typed popups): In scripts/vscripts/libraries/popups.lua, dedicated popup helpers give each event a distinct color, pre/post symbol and lifetime: PopupCriticalDamage uses red + a lightning post-symbol, PopupHealing green + plus, PopupGoldGain gold + plus (team-only). The primary 'juice' layer for combat readability.
      function PopupCriticalDamage(target, amount)
        PopupNumbers(target, "crit", Vector(255, 0, 0), 3.0, amount, nil, POPUP_SYMBOL_POST_LIGHTNING)
      end
    - Full-screen kill banners (Rampage / multi-kill): In scripts/vscripts/libraries/popups.lua, big screen-space feedback for streaks via econ killbanner particles attached to PATTACH_EYES_FOLLOW (screen-locked). Names map to firstblood/doublekill/triplekill/rampage/multikill_generic.
      local particleName = "particles/econ/events/killbanners/screen_killbanner_compendium14_"..name..".vpcf"
      local particle = ParticleManager:CreateParticle( particleName, PATTACH_EYES_FOLLOW, target )
    - Leap / charge via per-frame SetAbsOrigin motion (Life Break): In scripts/vscripts/heroes/hero_huskar/life_break.lua, movement-impact feel is produced with horizontal+vertical motion controllers that nudge AbsOrigin each frame (parabolic z arc up then down), break on reaching the target, snap to ground, then fire impact sound + particle. InterruptMotionControllers ends the leap cleanly. The engine's stand-in for knockback/hit-react motion.
      caster:SetAbsOrigin(caster:GetAbsOrigin() + direction * ability.velocity)
      -- vertical arc:
      ability.life_break_z = ability.life_break_z + ability.velocity/2
      caster:SetAbsOrigin(caster_loc_ground + Vector(0,0,ability.life_break_z))
    - Instant position swap with FindClearSpaceForUnit (Spectre reality): In scripts/vscripts/heroes/hero_spectre/haunt.lua, teleport/swap feel: set both units' forward vectors, swap AbsOrigin, then FindClearSpaceForUnit(unit, pos, true) on each to de-overlap, plus an impact sound. The clear-space call is what makes blink/swap reads feel solid rather than stacked.
      caster:SetAbsOrigin(illusionPos)
      spectre:SetAbsOrigin(casterPos)
      FindClearSpaceForUnit(caster, illusionPos, true)
      FindClearSpaceForUnit(spectre, casterPos, true)
      EmitSoundOn("Hero_Spectre.Reality", caster)
    - Multiplier / duel floating text on top of unit: In scripts/vscripts/libraries/popups.lua, reuses unstable-concoction-timer and legion-duel-text particles as big stylized numbers floating ~322 units above a unit's head (CP packed with digits + symbol + world position), for emphatic combo/duel feedback distinct from normal damage numbers.
      local particle = ParticleManager:CreateParticle( particleName, PATTACH_CUSTOMORIGIN, target )
      ParticleManager:SetParticleControl(particle, 0, Vector(targetPos.x, targetPos.y, targetPos.z+322))
      ParticleManager:SetParticleControl( particle, 1, Vector( preSymbol, number, postSymbol) )
  AI/COMBAT:
    - Utility/desire behavior system (AICore): Reusable AI brain: CreateBehaviorSystem({behaviors}) runs a think loop that each tick calls Evaluate() on every behavior and picks the highest desire score; selected behavior gets Begin/Continue/End/Think hooks and an order table that is re-issued every think (repeatedlyIssueOrders) to survive dropped orders. Helpers RandomEnemyHeroInRange and WeakestEnemyHeroInRange wrap FindUnitsInRadius for targeting. (scripts/vscripts/ai_core.lua)
    - Boss 'push the Ancient' behavior: Map boss AI built on AICore: SetContextThink('AIThink', 0.25) drives behaviorSystem:Think(); BehaviorAttackAncient:Evaluate counts living non-AA friendly creeps in radius (FindUnitsInRadius, radius -1 = whole map) and raises desire to 3 when fewer than 3 allies remain, then issues a 5s ATTACK_MOVE order toward a lane waypoint (lane_bot_pathcorner_goodguys_2). AIThink returns nil to self-deactivate when the unit dies. (scripts/vscripts/friendai.lua)
    - Thinker projectile with detect-and-launch (Spark Wraith): A modifier thinker arms after startup_time, then OnIntervalThink(0) scans FindUnitsInRadius(... DOTA_UNIT_TARGET_CREEP + HERO, FIND_CLOSEST) and, on first enemy found, fires a CreateTrackingProjectile from source attachment 1 toward it (with vision provided), then tears itself down. Classic auto-turret/seeking-mine combat AI. (scripts/vscripts/heroes/hero_arc_warden/arc_warden_spark_wraith_lua.lua)
    - Bouncing tracking-projectile targeting (Death Ward): Bounce AI on a dummy unit: FindUnitsInRadius(FIND_CLOSEST) within bounceRange, skip the current target and any already in bounceTable (so it doesn't repeat), require CanEntityBeSeenByMyTeam, then CreateTrackingProjectile (iSourceAttachment = DOTA_PROJECTILE_ATTACHMENT_HITLOCATION) to the next target; damage decays per bounce and the dummy self-removes when maxBounces is hit or no target remains. (scripts/vscripts/heroes/hero_witch_doctor/death_ward.lua)
    - Dummy-unit-follows-particle linear projectile (Chaos Meteor): Projectile-pattern technique: spawn an invisible npc_dummy_unit that carries the damage modifier, vision, and looping sound, create a CreateLinearProjectile with the meteor .vpcf, and advance the dummy's AbsOrigin every 0.03s in lockstep with the projectile velocity; nested Timers stage impact -> burn-DOT -> linger-vision -> RemoveSelf. AOE damage ticks via FindUnitsInRadius around the dummy. (scripts/vscripts/heroes/hero_invoker/chaos_meteor.lua)
    - Stationary wall of zero-velocity linear projectiles (Macropyre): Area-denial pattern: compute numProjectiles along a line and spawn a row of CreateLinearProjectile with vVelocity = Vector(0,0,0) and overlapping fStartRadius/fEndRadius (pathRadius*sqrt2) so they form a persistent damaging strip until fExpireTime; per-target damage is gated by a DistancePointSegment check against the path so units are only hit inside the line, and dummies destroy trees along it. (scripts/vscripts/heroes/hero_jakiro/macropyre.lua)

## Angel Arena Reborn (Dota 2 custom game, workshop id 500020226)
  PARTICLES:
    - CreateParticle + SetParticleControlEnt with named attach (hitloc): Canonical wiring pattern: create a particle parented to a unit (PATTACH_OVERHEAD_FOLLOW / PATTACH_ABSORIGIN_FOLLOW), then bind a control point to an entity attachment by name ("attach_hitloc") with SetParticleControlEnt so the effect tracks a bone, not just the origin. Re-uses stock valve econ vpcf paths (warlock fatal bonds) rather than custom assets. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\boss\abyss_warrior\abyss_warrior_ethereal_charge.lua)
      local idx = ParticleManager:CreateParticle("particles/econ/items/warlock/warlock_ti10_head/warlock_ti_10_fatal_bonds_cast.vpcf", PATTACH_OVERHEAD_FOLLOW, caster)
      ParticleManager:SetParticleControlEnt( idx, 1, caster, PATTACH_ABSORIGIN_FOLLOW, "attach_hitloc", casterPos, true)
    - Fire-and-forget world particle: SetParticleControl + immediate ReleaseParticleIndex: For one-shot impact effects at a world point they create with PATTACH_CUSTOMORIGIN/PATTACH_WORLDORIGIN (parent nil), set CP3/CP0 to vLocation, then immediately ReleaseParticleIndex so the engine GCs the index once the effect finishes. This is their leak-free pattern for transient hits. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\abilities\golems\golem_big_throw_rock.lua)
      local particle = ParticleManager:CreateParticle( "particles/neutral_fx/mud_golem_hurl_boulder_explode.vpcf", PATTACH_CUSTOMORIGIN, nil )
      ParticleManager:SetParticleControl( particle, 3, vLocation )
      ParticleManager:ReleaseParticleIndex( particle )
    - AOE radius encoded in a control-point vector: Persistent ground-effect (firestorm pre) where CP0 is the world point and CP1 packs the radius into a Vector(radius,1,1) so the vpcf scales itself to the ability's actual AOE. Released after spawn. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\abilities\megacreep_golem\megacreep_ancient_golem_meteor_rain.lua)
      local part = ParticleManager:CreateParticle("particles/units/heroes/heroes_underlord/underlord_firestorm_pre.vpcf", PATTACH_WORLDORIGIN, nil)
      ParticleManager:SetParticleControl( part, 0, point )
      ParticleManager:SetParticleControl( part, 1, Vector( radius, 1, 1 ) )
      ParticleManager:ReleaseParticleIndex( part )
    - Modifier-owned persistent particle via GetEffectName / GetEffectAttachType: Buff/aura visuals are declared on the modifier through GetEffectName + GetEffectAttachType instead of manual create/release — the engine attaches on apply and tears down on modifier destroy automatically, the clean idiomatic way to bind a looping effect to a buff's lifetime. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\boss\keymaster\modifiers\modifier_keymaster_damned_souls.lua)
      function mod:GetEffectName() 	return "particles/econ/courier/courier_trail_orbit/courier_trail_orbit.vpcf" end
      function mod:GetEffectAttachType() 	return PATTACH_ABSORIGIN_FOLLOW end
    - Particle teleport beam: two SetParticleControlEnt with named attach points: Void-spirit astral-step beam wired between two world endpoints by binding CP0 to the source attachment ("start_at_attachment") and CP2 to the destination — used to draw a blink/dash trail for a creep flash. (Note the latent bug: parent is nil here, an undeclared global — a real shipped-code artifact.) (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\abilities\megacreep_energy_creature\megacreep_energy_creature_flash.lua)
      local particle = ParticleManager:CreateParticle("particles/units/heroes/hero_void_spirit/astral_step/void_spirit_astral_step_beam.vpcf", PATTACH_WORLDORIGIN, parent)
      ParticleManager:SetParticleControlEnt( particle, 0, parent, PATTACH_WORLDORIGIN, "start_at_attachment", caster:GetAbsOrigin(), true)
      ParticleManager:SetParticleControlEnt( particle, 2, parent, PATTACH_WORLDORIGIN, "start_at_attachment", pos, true)
    - Dynamic data fed through control point: medusa mana-shield impact scaled by mana spent: On-take-damage they spawn the impact vpcf at the caster and feed gameplay numbers in via control points: CP0 = position, CP1 = Vector(mana_needed,0,0). Particle visual intensity is driven by live combat math rather than a static effect. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\heroes\medusa\modifier_medusa_mana_shield_2.lua)
      local particle = ParticleManager:CreateParticle(particleName, PATTACH_ABSORIGIN_FOLLOW, caster)
      ParticleManager:SetParticleControl(particle, 0, caster:GetAbsOrigin())
      ParticleManager:SetParticleControl(particle, 1, Vector(mana_needed,0,0))
  SOUNDS:
    - EmitSound on caster keyed to gameplay outcome (Keymaster Bite): Different soundevents fired by result: a Cast sound on every hit, plus a distinct Kill sound only when the execute threshold triggers. Sounds reuse Valve hero soundevent names (Boss_Keymaster.* are custom-registered events) so design communicates state through audio branching. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\boss\keymaster\keymaster_bite.lua)
    - Looping sound lifecycle: Emit on create, StopSoundOn + Emit end-sound on destroy: Charon Collapse emits START_SOUND and a LOOP_SOUND together on modifier OnCreated, then on OnDestroy calls StopSoundOn(LOOP_SOUND, caster) and plays END_SOUND. This start/loop/stop triad tied to modifier lifetime is their standard channel-sound cleanup, preventing orphaned loops. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\heroes\charon\charon_collapse\modifier_charon_collapse_in_caster.lua)
    - Per-hero soundevent files wired in npc_units_custom.txt: Units declare GameSoundsFile pointing at stock Valve vsndevts (game_sounds_dragon_knight, game_sounds_lycan, game_sounds_creeps, etc.) so custom heroes/creeps inherit existing hero audio banks instead of shipping new .vsndevts. No custom soundevents file exists on disk in the decompile. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\npc\npc_units_custom.txt)
    - Positional sound helpers: EmitSoundOnLocationWithCaster / EmitSoundOn: They mix attachment styles: EmitSoundOn("Hero_Lina.DragonSlave", caster) for caster-anchored casts and EmitSoundOnLocationWithCaster(caster:GetAbsOrigin(), "Hero_Winter_Wyvern.SplinterBlast.Cast", nil) for world-located SFX on a blink, letting audio spatialize at the impact point rather than follow the unit. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\abilities\megacreep_energy_creature\megacreep_energy_creature_flash.lua)
    - Shared StopSound wrapper over StopSoundEvent: A small heroes/sounds.lua wrapper exposes StopSound(keys) -> StopSoundEvent(sound, target), giving a single choke point for stopping data-driven sound events from KV ability blocks rather than scattering StopSoundEvent calls. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\heroes\sounds.lua)
    - Panorama UI sound via Game.EmitSound: The repick/hero-select menu plays a UI feedback sound on hero confirm with Game.EmitSound("ui_select_md") directly in JS when the pick button becomes visible — client-side audio feedback tied to a UI state change. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\panorama\scripts\custom_game\repick_menu.js)
  GAME-FEEL:
    - Floating combat text via SendOverheadEventMessage: No custom damage-number panel; they lean on the engine's overhead alert system for heal/mana feedback. OVERHEAD_ALERT_HEAL pops green heal numbers, OVERHEAD_ALERT_MANA_LOSS shows mana burn. Used pervasively across heroes/bosses (abyss warrior heal, joeblack lifesteal, shadowsong mana burn) for readable combat feedback.
      SendOverheadEventMessage(unit, OVERHEAD_ALERT_HEAL, unit, ammount, nil)
    - Knockback via custom HorizontalMotionController (no engine knockback): Soul Guardian heavy-strike implements knockback by hand: compute direction from impact point, derive step = radius/duration, ApplyHorizontalMotionController, then in UpdateHorizontalMotion move the parent step*dt each tick via SetAbsOrigin; OnHorizontalMotionInterrupted / OnDestroy call InterruptMotionControllers to release cleanly. This is their reusable displacement pattern.
      self.step = self.radius / self.duration
      self.dir = (self.target:GetAbsOrigin() - self.point):Normalized()
      local motion = self:ApplyHorizontalMotionController()
      -- per tick:
      local step = self.dir * (self.step * dt)
      self:GetParent():SetAbsOrigin(origin + step)
    - AOE knockback seeded from cursor point, with tree clearing: Harpy knockback finds units in radius around the cursor point, applies a LUA_MODIFIER_MOTION_HORIZONTAL push modifier carrying the origin point as a stringified Vector kv, and calls GridNav:DestroyTreesAroundPoint so displaced units aren't stuck — a polish touch for clean motion feel.
      enemy:AddNewModifier(caster, self, "modifier_harpy_knockback_active", { radius = radius, dur = duration, duration = duration, point = Vector(point.x, point.y, point.z)})
      GridNav:DestroyTreesAroundPoint(point, radius, true)
    - Blink/teleport feel via FindClearSpaceForUnit + paired start/end particles: Dark Reflection swaps caster and target positions: FindClearSpaceForUnit on both, paired ti4 blink-start particles on each at AbilityPhaseStart, and end-sparkle particles, plus a Hero_Antimage.Counterspell.Cast sound — layering pre-cast and on-cast VFX to sell instantaneous repositioning.
      FindClearSpaceForUnit(caster, targetPos, false)
      FindClearSpaceForUnit(target, casterPos, false)
      ParticleManager:CreateParticle("particles/econ/events/ti4/blink_dagger_start_ti4.vpcf", PATTACH_ABSORIGIN, caster)
    - Hidden caster (AddNoDraw) during a collapse channel: Charon Collapse pulls the caster off-screen with caster:AddNoDraw() while a fullscreen-style charon_collapse_1 vpcf plays at the stored caster position, with looping audio — a cinematic 'vanish into the effect' game-feel beat rather than a screen shake/vignette (the engine ScreenShake API is not used anywhere in this codebase).
      caster:AddNoDraw()
      self.inCasterParticle = ParticleManager:CreateParticle("particles/charon/charon_collapse/charon_collapse_1.vpcf", PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControl(self.inCasterParticle, 0, self.caster_position)
  AI/COMBAT:
    - Abyss Warrior boss AI — leash + priority decision tick: Full standalone think-AI in Spawn(): caches abilities/ranges, runs a 0.5s Timers loop. Each tick it leashes back to spawnPos if it strays past BACK_TO_SPAWN_RANGE, picks the nearest visible target via FindUnitsInRadius (820 search), and prioritizes ability casts in order — Death Link only when 2+ enemies cluster within link_radius, Domination on the lowest-HP target in range, Ethereal Charge as a self-heal when below 70% HP — else MoveToTargetToAttack. Guards against re-casting while IsInAbilityPhase. Textbook utility/priority boss controller. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\boss\abyss_warrior\abyss_warrior_ai.lua)
    - Keymaster Damned Souls — staged projectile-volley summon modifier: Modifier-driven multi-projectile barrage: a Timers loop fires soul_count tracking projectiles at tickInterval, each tick re-running FindUnitsInRadius(FIND_CLOSEST) to retarget the nearest enemy. Tracks live projectiles in self.projs and counts nProjs, applies a debuff + bonus damage scaling with target's lost HP on hit, and self-destroys only after all projectiles land AND all ticks fired. Includes a real shipped-game comment about a Dota crash when destroying in-flight projectiles. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\boss\keymaster\modifiers\modifier_keymaster_damned_souls.lua)
    - Golem Big — tracking projectile with OnProjectileHit gating: ProjectileManager:CreateTrackingProjectile (mud_golem_hurl_boulder) toward cursor target; OnProjectileHit validates the target (not invulnerable, TriggerSpellAbsorb check) before applying a stun modifier + damage and spawning a released impact particle. Clean canonical creep nuke-projectile lifecycle. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\abilities\golems\golem_big_throw_rock.lua)
    - Linear (skillshot) projectile pattern — Fiery Stream: ProjectileManager:CreateLinearProjectile with vVelocity = speed*direction, fStartRadius/fEndRadius width, fDistance, and unit-target team/type filters; OnProjectileHit applies damage and a slow debuff, grants caster a buff only when it hits a real hero, and returns false to let the projectile pierce. Shows their pierce-vs-stop convention via the boolean return. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\heroes\fireblade\fireblade_fiery_stream.lua)
    - Meteor Rain — point-targeted AOE with FindUnitsInRadius: GetAOERadius drives the cursor reticle; OnSpellStart does FindUnitsInRadius(team ENEMY, HERO+BASIC) at the cursor point and ApplyDamage in a loop, then spawns an underlord firestorm vpcf scaled by radius. Representative of their many ground-target boss/megacreep nukes. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\abilities\megacreep_golem\megacreep_ancient_golem_meteor_rain.lua)
    - Visibility-aware targeting helper (_CanBeSeen) shared by boss AI: Targeting filters in the AI reject units that can't be seen by team, are invisible, invulnerable, or couriers, and the enemy search uses DOTA_UNIT_TARGET_FLAG_MAGIC_IMMUNE_ENEMIES + NO_INVIS — so bosses behave fairly (no targeting through fog/invis) and won't waste casts on couriers. A reusable aggro-validation idiom. (C:\Users\work\.dota2-workshop-mcp\reflib\items\500020226\files\scripts\vscripts\creeps\boss\abyss_warrior\abyss_warrior_ai.lua)

## Life In Arena (id 407750024) — co-op survival/tower-defense Dota 2 custom game. Players defend against scripted enemy "waves" (1..19), elite "bosses" and "megabosses". Source layout: scripts/vscripts/{abilities, ai, heroes/<hero>}, panorama/{layout,scripts,styles}/custom_game. Combat/FX is almost entirely server-side Lua (ParticleManager + EmitSound); panorama is HUD-only (toasts, scoreboard, timer) with NO DOTAParticleScenePanel or UI sounds. Note: enemy AI think loops `require('survival/AIcreeps')` and call AICreepsAttackOneUnit/use a global Survival.AICreepCasts cast-budget, but the survival/ library itself was NOT extracted into this file set (only the per-wave think wrappers in scripts/vscripts/ai are present).
  PARTICLES:
    - ReleaseParticleIndex for fire-and-forget one-shot FX (multi control-point hit effect): Canonical transient-FX pattern: CreateParticle, wire several control points (CP0 origin-follow, CP1 attach_hitloc point-follow, CP2 caster), then immediately ReleaseParticleIndex so the engine GCs it when the effect ends. Used for on-hit impacts. File: abilities/second_wave_wave_of_force.lua:59-63. (scripts/vscripts/abilities/second_wave_wave_of_force.lua)
      local nFXIndex = ParticleManager:CreateParticle( "...magnataur_shockanvil_hit.vpcf", PATTACH_CUSTOMORIGIN, hTarget )
      ParticleManager:SetParticleControlEnt( nFXIndex, 0, hTarget, PATTACH_ABSORIGIN_FOLLOW, nil, hTarget:GetOrigin(), true )
      ParticleManager:SetParticleControlEnt( nFXIndex, 1, hTarget, PATTACH_POINT_FOLLOW, "attach_hitloc", hTarget:GetOrigin(), true )
      ParticleManager:SetParticleControlEnt( nFXIndex, 2, self:GetCaster(), PATTACH_ABSORIGIN_FOLLOW, nil, hTarget:GetOrigin(), true )
      ParticleManager:ReleaseParticleIndex( nFXIndex )
    - Modifier-owned particle via self:AddParticle (auto-cleanup on modifier remove): Persistent FX attached for a modifier's lifetime: CreateParticle with PATTACH_CUSTOMORIGIN_FOLLOW, bind CP0 to an attach point, then register it with self:AddParticle so it's destroyed automatically when the modifier ends (no manual ReleaseParticleIndex / OnDestroy needed). File: heroes/android/android_cluster_rockets.lua:75-77. (scripts/vscripts/heroes/android/android_cluster_rockets.lua)
      local fx = ParticleManager:CreateParticle("...hero_tinker/tinker_rearm.vpcf", PATTACH_CUSTOMORIGIN_FOLLOW, caster)
      ParticleManager:SetParticleControlEnt(fx, 0, caster, PATTACH_CUSTOMORIGIN_FOLLOW, "attach_attack1", Vector(0,0,0), false)
      self:AddParticle(fx, false, false, 0, false, false)
    - Explicit DestroyParticle paired with StopSound in OnDestroy (aura thinker): For a long-lived aura/zone: cache the particle handle on the modifier in OnCreated (with EmitSound), and in OnDestroy call ParticleManager:DestroyParticle(handle, true) plus StopSound to tear down both FX and looping audio together. Clean lifecycle pairing. File: heroes/darkknight/modifier_dark_knight_dark_energy_zone_thinker.lua:59-75. (scripts/vscripts/heroes/darkknight/modifier_dark_knight_dark_energy_zone_thinker.lua)
      function ...zone_thinker:OnCreated(kv)
        self.DarkEnergyZoneParticle = ParticleManager:CreateParticle("...dark_energy_zone.vpcf",PATTACH_ABSORIGIN,self:GetParent())
        ParticleManager:SetParticleControl(self.DarkEnergyZoneParticle,1,Vector(self.radius,self.radius,self.radius))
        self:GetParent():EmitSound("Hero_Abaddon.BorrowedTime")
      end
      function ...zone_thinker:OnDestroy()
        ParticleManager:DestroyParticle(self.DarkEnergyZoneParticle,true)
        self:GetParent():StopSound("Hero_Abaddon.BorrowedTime")
      end
    - Overhead stack-counter particle driven by SetParticleControl per stack: A persistent overhead 'charges' particle (cached as caster.shootingStarOverhead) where each stack toggles a control-point Vector(1,0,0)=lit / Vector(0,0,0)=off. A helper loops CPs 1..7 to match stack count, and SetParticleStackCount syncs the visual. Shows using control points as a discrete UI/state display on a unit. File: heroes/archmage/archmage_shooting_star.lua:92,253-258. (scripts/vscripts/heroes/archmage/archmage_shooting_star.lua)
      for i=1,7 do
        if i <= nStackCount then
          ParticleManager:SetParticleControl(pIndex, i, Vector(1,0,0))
        else
          ParticleManager:SetParticleControl(pIndex, i, Vector(0,0,0))
        end
      end
    - Radius-scaled ground AoE FX via control point 4: Ground-targeted wave effect spawned at PATTACH_CUSTOMORIGIN with no parent entity: CP0 = world position, CP4 = Vector(radius,1,1) to scale the ring/AoE to the ability radius. Pattern for telegraphing AoE size with a single particle. File: abilities/5_wave_firestorm.lua:49-51. (scripts/vscripts/abilities/5_wave_firestorm.lua)
      local firestorm = ParticleManager:CreateParticle(particleName, PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControl(firestorm, 0, target_position)
      ParticleManager:SetParticleControl(firestorm, 4, Vector(radius, 1, 1))
    - Centralized particle/sound precache on game init: All ability FX + soundevent files are precached in one block in the addon's Precache(context) using PrecacheResource('particle', ...) and PrecacheResource('soundfile', 'soundevents/.../game_sounds_*.vsndevts'), preventing first-cast hitching. ~dozens of entries. File: addon_game_mode.lua:49-66. (scripts/vscripts/addon_game_mode.lua)
      PrecacheResource("particle", "particles/units/heroes/hero_slardar/slardar_crush.vpcf", context)
      PrecacheResource("soundfile", "soundevents/game_sounds_heroes/game_sounds_naga_siren.vsndevts", context)
  SOUNDS:
    - EmitSoundOn(name, unit) for positional cast/impact SFX: Standard reuse of stock Dota hero soundevents on custom units: cast sound on caster, target/impact sound on victim. Magnataur shockwave example fires Cast.Anvil on caster and ShockWave.Target on each hit unit. Pervasive across abilities. (scripts/vscripts/abilities/second_wave_wave_of_force.lua)
    - self:GetParent():EmitSound / StopSound for looping aura audio: Looping soundevent started in modifier OnCreated and explicitly StopSound'd in OnDestroy so the loop never leaks past the buff (dark energy zone uses Hero_Abaddon.BorrowedTime). The only StopSound usages in the codebase are these aura/zone teardowns. (scripts/vscripts/heroes/darkknight/modifier_dark_knight_dark_energy_zone_thinker.lua)
    - MODIFIER_PROPERTY_TRANSLATE_ATTACK_SOUND + GetAttackSound() to reskin auto-attacks: Butcher rage modifier declares TRANSLATE_ATTACK_SOUND and returns 'Hero_Pudge.DismemberSwings' from GetAttackSound(), swapping the unit's normal attack audio while the buff is active (paired with GetEffectName for a trail particle). Clean way to theme attacks without new soundevents. (scripts/vscripts/heroes/butcher/modifier_butcher_rage.lua)
    - Reused stock impact sounds for hit feedback (Roshan.Bash, Gyro rocket impact): Bash/stun procs play EmitSoundOn('Roshan.Bash', attacker) and rocket impacts play 'Hero_Gyrocopter.Rocket_Barrage.Impact' on the target — borrowing recognizable Dota SFX for crunchy hit confirmation rather than authoring custom .vsndevts. (scripts/vscripts/abilities/megaboss_5_bash.lua)
    - Precached custom soundevent files (game_sounds_*.vsndevts): addon_game_mode.lua Precache block loads soundevents/game_sounds_heroes/game_sounds_{slardar,morphling,naga_siren,chaos_knight}.vsndevts etc., so reskinned hero abilities can EmitSound those events on custom NPC units. (No standalone .vsndevts source files were extracted into this dump — only the precache references.) (scripts/vscripts/addon_game_mode.lua)
    - No panorama/UI sounds: Grep found zero PlayUISoundEvent / Game.EmitSound / CSS sound usage in panorama; all audio is server-side EmitSound on entities. UI feedback (kills, boss kills, trades) is purely visual toasts. Worth noting as a deliberate scope boundary. (panorama/scripts/custom_game/combat_events.js)
  GAME-FEEL:
    - Grid-aware radial knockback via SetAbsOrigin interval-think modifier: Custom knockback (modifier_knockback_lia, abilities/modifiers/modifier_knockback_lia.lua) computes a target point radially outward from a center, then every 0.03s nudges the unit toward it with SetAbsOrigin. It HALTS on GridNav blocked/non-traversable (reverts to last good pos and Destroy), and on destroy calls FindClearSpaceForUnit + ResolveNPCPositions and applies modifier_phased to avoid getting stuck. CheckState locks STUNNED + NO_UNIT_COLLISION; GetOverrideAnimation returns ACT_DOTA_FLAIL for the tumble feel. This is the main motion/game-feel primitive.
      function modifier_knockback_lia:OnIntervalThink()
        local position = self.unit:GetAbsOrigin()
        local direction = (self.target - position):Normalized()
        local delta = (distance * 1.0 / self.duration) * 1.0 / 30.0
        local next_position = position + direction * delta
        if GridNav:IsBlocked(next_position) or not GridNav:IsTraversable(next_position) then
          self.unit:SetAbsOrigin(self.old_position); self:Destroy()
        else
          self.old_position = self.unit:GetAbsOrigin(); self.unit:SetAbsOrigin(next_position)
        end
      end
    - Charged-channel wind-up via GetPlaybackRateOverride: Berserker fire spear (heroes/berserker/berserker_fire_spear.lua) is a channel whose throw damage scales with channel fraction; the cast animation playback rate is slowed (returns 0.21 normally, 0.42 in 'Madness') so the wind-up reads as a heavier, charged throw. Also throws on interrupt (OnChannelFinish computes elapsed/full ratio and still fires). The only time-manipulation game-feel in the game — there is NO global hit-stop or GameRules:SetTimeScale.
      function berserker_fire_spear:GetPlaybackRateOverride()
        local mad = self:GetCaster():FindModifierByName("modifier_berserker_madness")
        if mad then return 0.42 end
        return 0.21
      end
    - Floating combat-event toasts (no engine OverheadEvent for kills): Kill / boss-kill / duel / trade feedback is a custom Panorama toast system (panorama/scripts/custom_game/combat_events.js): Lua fires GameEvents 'lia_create_toast', JS builds a row from a layout snippet, adds AllyEvent/EnemyEvent/NeutralEvent classes (drives color/animation in CSS), and auto-collapses after 10s via $.Schedule. Local-player-involved events get a LocalPlayerInvolved highlight class.
      var newToast = $.CreatePanel("Panel", toastManager, "");
      newToast.BLoadLayoutSnippet("CombatEventRow");
      newToast.AddClass("ToastVisible");
      $.Schedule(10, function(){ newToast.RemoveClass("ToastVisible"); newToast.AddClass("Collapse"); });
    - Engine OverheadEventMessage for gold/heal floaters: Numeric floating text (lifesteal heal amounts in abilities/wave_8_lifesteal_aura.lua, dominate bounty gold) uses the built-in SendOverheadEventMessage(playerOwner, OVERHEAD_ALERT_HEAL / OVERHEAD_ALERT_GOLD, unit, value, nil) rather than custom particles — cheap stock damage/heal numbers over the unit.
      SendOverheadEventMessage(params.attacker:GetPlayerOwner(), OVERHEAD_ALERT_HEAL, params.attacker, lifesteal, nil)
    - Reusable dummy-unit pattern for ground-targeted FX/spawns: Ground abilities create a temporary dummy_unit_vulnerable at the target point to anchor thinker modifiers (firestorm rain) or to give tracking projectiles a target to home on (cluster rockets, heroes/android/android_cluster_rockets.lua, aim at randomly-offset dummies removed after 3s). Lets positional AoE reuse unit-attached particle/modifier machinery and feel like it 'lands'.
      local dummy = CreateUnitByName("dummy_unit_vulnerable", random_position, false, caster, caster, DOTA_UNIT_TARGET_TEAM_ENEMY)
      ProjectileManager:CreateTrackingProjectile({ ... Target = dummy ... })
      Timers:CreateTimer(3, function() dummy:RemoveSelf() end)
    - No screen shake / camera control: Grep across the entire addon found ZERO ScreenShake, SetCameraTarget, camera-distance, vignette or flash usage. Game-feel is delivered entirely through unit motion (SetAbsOrigin knockback), animation pacing, particles, sound, and HUD toasts — a deliberately lightweight feel layer.
  AI/COMBAT:
    - Per-unit SetContextThink wave AI loop (creep/boss): Each enemy unit's Spawn() caches ability handles and registers thisEntity:SetContextThink(name, ThinkFn, 0.1). The think fn early-returns nil if dead/illusion, returns 1 while game paused, calls the shared AICreepsAttackOneUnit({unit=thisEntity}) for basic aggro/attack, returns 2 (re-think delay) while stunned, otherwise evaluates ability casts. Pattern repeated for every wave 1..19 and bosses. (scripts/vscripts/ai/6_wave_bosses.lua)
    - Global cast-budget throttle (Survival.AICreepCasts < AIMaxCreepCasts): All AI ability casts are gated by a shared global counter incremented on each cast, capping how many creeps can cast per tick across the whole wave — prevents the swarm from chain-CCing players and smooths difficulty. The throttle lives in the un-extracted survival/AIcreeps library but is read/written by every ai/*_wave_*.lua think loop. (scripts/vscripts/ai/12_wave_bosses.lua)
    - FindUnitsInRadius targeting with debuff/state filtering: Targeted-cast AI does FindUnitsInRadius(enemy team, origin, ~600-700, HERO[+BASIC]), then table.remove's units that already have the relevant debuff or are stunned, and casts on a random survivor via CastAbilityOnTarget(target, ability, -1). Uses DOTA_UNIT_TARGET_FLAG_MANA_ONLY to prefer mana-having targets (roots). Avoids wasting casts on already-affected units. (scripts/vscripts/ai/12_wave_bosses.lua)
    - HP-percent boss phase gating: Boss think branches on thisEntity:GetHealthPercent(): e.g. wave 12 boss self-buffs Bloodlust only while HP >= 25%, otherwise falls through to its Roots crowd-control behavior — a simple two-phase boss state machine driven by health thresholds. megaboss_* abilities add the elite-tier modifiers (bash/crit/mana_break) layered on bosses. (scripts/vscripts/ai/12_wave_bosses.lua)
    - Tracking projectile barrage homing on spawned dummies: Android cluster rockets: a modifier StartIntervalThink fires one rocket per interval; each FireRocket spawns a dummy at target_position + RandomVector(RandomInt(0,radius)) and CreateTrackingProjectile homes on it (DOTA_PROJECTILE_ATTACHMENT_ATTACK_1). OnProjectileHit does FindUnitsInRadius AoE damage + stun and spawns an impact particle. Models a scatter/barrage projectile pattern. (scripts/vscripts/heroes/android/android_cluster_rockets.lua)
    - Piercing linear projectile with cached per-shot damage: Berserker fire spear / wave_of_force use ProjectileManager:CreateLinearProjectile with bDeleteOnHit=false (returns false in OnProjectileHit to keep piercing), caching damage/slow on the ability before firing and applying them per-hit. wave_of_force scales damage by % of target max health. Shows linear-vs-tracking projectile design choices and pierce handling. (scripts/vscripts/heroes/berserker/berserker_fire_spear.lua)

## Random Farm TD (Dota 2 custom game, workshop id 2052179257) - a co-op maze/lane tower-defense where each player farms gold, rolls random hero-tower units, and survives scaling enemy waves plus three activatable "bosses". Reverse-engineered from decompiled Lua (scripts/vscripts) and Panorama (panorama/scripts). No custom .vsndevts or .vpcf source ship in the dump: it reuses stock Dota hero soundevents/particles by path, plus a bundled AMHC "floating number" particle set (particles/msg_fx/*.vpcf).
  PARTICLES:
    - Multi-particle AoE burst at fixed XY offsets (firestorm): hellfire.lua spawns the SAME stock vpcf 5 times around the target, hand-offsetting each by +/-200 on X/Y via SetParticleControl CP0 to fake a wide field of explosions cheaply. Created inside a repeating Timer that also re-runs FindUnitsInRadius for damage. Attach is PATTACH_ABSORIGIN (world-locked, not following). (scripts/vscripts/abilities/units_abilities/hellfire.lua)
      local hellfire_particle = ParticleManager:CreateParticle("particles/units/heroes/heroes_underlord/abyssal_underlord_firestorm_wave_chunks_a.vpcf", PATTACH_ABSORIGIN, target)
      ParticleManager:SetParticleControl(hellfire_particle, 0, Vector(target_abs.x+200, target_abs.y+200, target_abs.z))
    - SetParticleControlMassPoints helper (broadcast one origin to many CPs): ExcaliburS:SetParticleControlMassPoints loops a control-point list and sets them all to the same world origin. Used to stack 4 separate stock gyrocopter explosion vpcfs on an impact, feeding required CPs (0,1,2,3,4,9,11,12) without writing each call. The reusable pattern for 'I don't know which CPs the vpcf needs, set them all'. (scripts/vscripts/libraries/excaliburs.lua)
      function ExcaliburS:SetParticleControlMassPoints(particleIndex, abs, cPointsTbl)
        for _, cp in pairs(cPointsTbl) do
          ParticleManager:SetParticleControl(particleIndex, cp, abs)
        end
      end
      -- caller: ExcaliburS:SetParticleControlMassPoints(target.explosive_particle, target:GetAbsOrigin(), {0,1,2,3,4,11,12})
    - Timed particle cleanup pattern (Timer keyed by particle index): Persistent FX (blizzard field, poison clouds, slow debuffs) are created then a Timers:CreateTimer keyed off tostring(particleIndex) destroys it after a fixed lifetime with ParticleManager:DestroyParticle(idx, false) and removes itself. ExcaliburS:CreateTemporaryParticle wraps this into a one-call helper with an optional callback. This is their universal 'fire-and-forget with guaranteed release' idiom. (scripts/vscripts/abilities/units_abilities/poison_area.lua)
      ParticleManager:DestroyParticle(poison_area_particle, false)
      StopSoundEvent("Hero_Pudge.Rot", target)
      Timers:RemoveTimer(tostring(UniqueTimerTime..ability_name))
    - Floating-number FX via control-point encoding (AMHC): Damage/gold/heal popups use a bundled particles/msg_fx/*.vpcf set. The number itself is encoded into control points: CP1 = Vector(10, number, icon_type), CP2 = Vector(duration, digit_count, 0), CP3 = RGB color. Attached PATTACH_CUSTOMORIGIN_FOLLOW to attach_hitloc so it tracks the unit. This is how they render arbitrary in-world numbers without Panorama. (scripts/vscripts/amhc_library/amhc.lua)
      local particle = AMHC:CreateParticle(msg_type,PATTACH_CUSTOMORIGIN_FOLLOW,false,entity,duration)
      ParticleManager:SetParticleControlEnt(particle,0,entity,5,"attach_hitloc",entity:GetOrigin(),true)
      ParticleManager:SetParticleControl(particle,1,Vector(10,number,icon_type))
      ParticleManager:SetParticleControl(particle,2,Vector(duration,number_count,0))
      ParticleManager:SetParticleControl(particle,3,color_vec)
    - Panorama building ghost + grid particles (CreateParticle / SetParticleControlEnt / ReleaseParticleIndex): building_helper.js drives the tower-placement preview entirely client-side: a ghost_model.vpcf with CP2=tint color, CP3=alpha, CP4=scale, attached to the cursor entity via SetParticleControlEnt; a grid of square_sprite.vpcf cells; and a strict teardown that calls DestroyParticleEffect(idx,true) THEN ReleaseParticleIndex(idx) for every tracked handle (model, grid, overlay, range) to avoid leaks. (panorama/scripts/building_helper.js)
      modelParticle = Particles.CreateParticle("particles/buildinghelper/ghost_model.vpcf", ParticleAttachment_t.PATTACH_CUSTOMORIGIN, 0);
      Particles.SetParticleControlEnt(modelParticle, 1, entindex, ParticleAttachment_t.PATTACH_ABSORIGIN_FOLLOW, "follow_origin", Entities.GetAbsOrigin(entindex), true)
      Particles.SetParticleControl(modelParticle, 4, [scale,0,0])
      // teardown: Particles.DestroyParticleEffect(p,true); Particles.ReleaseParticleIndex(p)
    - Panorama DOTAScenePanel for 3D tower preview: The glossary/tower-info window injects a DOTAScenePanel at runtime (renderdeferred, antialias, named map background, named light + camera) to show a live 3D model of the selected tower, then RemoveAndDeleteChildren() on close. Camera/map names are chosen per tower tier (divine pages compute a background_<type>_<page>). (panorama/scripts/custom_game/model_preview.js)
      CreateChildren(towerModelPreviewContainer, "DOTAScenePanel", "", scene_style, {hittest:false, renderdeferred: render_bg, antialias:true, map:map_name, particleonly:false, light:"light", camera:camera_name});
  SOUNDS:
    - StartSoundEvent / StopSoundEvent on world entity (ability SFX): Spell impacts reuse stock hero soundevents fired at the target entity, e.g. blizzard uses Ability.FrostNova, electric_shock uses Hero_Disruptor.ThunderStrike.Target, curse_down uses Hero_Nightstalker.Void, bombing uses Hero_Techies.Suicide, golden_ring uses DOTA_Item.Hand_Of_Midas. Looping clouds pair StartSoundEvent on cast with StopSoundEvent in the cleanup timer (e.g. Hero_Pudge.Rot). (scripts/vscripts/abilities/units_abilities/blizzard.lua)
    - EmitSound / StopSound on a projectile dummy unit: Projectile-dummy abilities EmitSound a looping travel sound on the dummy when spawned and explicitly StopSound it before killing the dummy on impact. Gyro rocket: dummy:EmitSound('Hero_Gyrocopter.HomingMissile') on spawn, dummy:StopSound(...) inside BoomGyroRocket; Tusk snowball: EmitSound('Hero_Tusk.Snowball.Cast') on spawn and 'Hero_Tusk.Snowball.ProjectileHit' on the knockback aura. (scripts/vscripts/abilities/units_abilities/gyro_attack_rocket/modifier_gyro_attack_rocket.lua)
    - EmitSoundOn / StopSoundOn for stacking DoT modifier: Huskar burning debuff plays Hero_Huskar.Burning_Spear with EmitSoundOn each time stacks are added (in AddStacks) and guarantees it stops via StopSoundOn in OnRemoved, so a unit losing all stacks/dying doesn't leave a dangling loop. (scripts/vscripts/abilities/units_abilities/huskar_split_shot/huskar_split_shot.lua)
    - EmitSoundOnEntityForPlayer (per-player sound) for notifications: Boss/cooldown-ready notifications and shop sells fire sounds only to the owning player so other players in the shared lobby don't hear them: e.g. ui.npe_objective_given when a boss cooldown is ready, and General.Sell on item sale. This is the per-player-scoped audio technique (vs StartSoundEvent which is global to listeners). (scripts/vscripts/items/first_boss_activate.lua)
    - Panorama Game.EmitSound for UI feedback: Every panel open/close/click routes through Game.EmitSound with stock UI events: ui_chat_slide_out (generic panel toggle, used everywhere), ui_menu_activate_open / ui_menu_activate_close (shop), General.LevelUp (chameleon ability confirm), General.Cancel, ui.profile_close. Consistent UI sound vocabulary across all custom panels. (panorama/scripts/custom_game/farmtd_shop.js)
    - Server-driven client sound bridge (EmitClientSound): container_base.js exposes EmitClientSound(msg) that calls Game.EmitSound(msg.sound) from a net message, letting Lua trigger arbitrary client-side UI sounds by name without a soundevent on the server. Paired with a dota_hud_error_message client event for HUD error feedback. (panorama/scripts/containers/container_base.js)
  GAME-FEEL:
    - Cinematic camera snap on spawn (SetCameraTarget then release): On hero spawn the camera is locked to the hero for a beat, then released back to free control after ~0.3s via a timer setting target to nil. AMHC:SetCamera wraps the same snap-then-release pattern with a 0.01s release. This gives a quick 'here is your base' framing without permanently locking the camera.
      PlayerResource:SetCameraTarget(playerID, hero)
      Timers:CreateTimer({ endTime = 0.30, callback = function()
        PlayerResource:SetCameraTarget(playerID, nil)
      end })
    - Smooth camera pan to a player's lane (Panorama lerp): Clicking a player entry in the scoreboard smoothly pans the camera to that player's farming area using GameUI.SetCameraTargetPosition(pos, 0.15) - the 0.15 is the lerp/transition time, so it eases rather than jump-cuts. Lets you spectate allies' lanes in this co-op TD.
      GameUI.SetCameraTargetPosition(playersAreasPos[id], 0.15);
    - Fixed top-down camera distance override: The whole game sets a single SetCameraDistanceOverride at init (via the boilerplate CAMERA_DISTANCE_OVERRIDE setting) to pull the camera back to a TD-appropriate height for the maze, instead of stock MOBA distance.
      mode:SetCameraDistanceOverride( CAMERA_DISTANCE_OVERRIDE )
    - Knockback / forced motion by SetAbsOrigin re-anchoring to a projectile: Tusk snowball 'pushes' enemies backward along the lane: the snowball dummy walks the player's waypoint chain in REVERSE, and a 0.03s timer on each caught unit hard-sets unit:SetAbsOrigin to the snowball's position every tick while a STUNNED+ACT_DOTA_FLAIL override animation sells the ragdoll/knockback feel. This is custom physics-free knockback.
      unit:SetAbsOrigin(snowball_abs)
      if snowball.current_corner then unit.current_corner = snowball.current_corner end
      return 0.03  -- (modifier_tuskar_snowball_knockback_aura_effect.lua, CheckState STUNNED + GetOverrideAnimation ACT_DOTA_FLAIL)
    - Floating damage / gold / crit / miss numbers (AMHC popups): Combat readability comes from AMHC:CreateNumberEffect spawning colored in-world numbers for damage, gold, heal, crit, evade, miss, mana, xp (each its own msg_fx vpcf), with caller-chosen color string and duration. E.g. golden_ring shows '+gold' in yellow over each enemy it converts. Stock SendOverheadEventMessage(OVERHEAD_ALERT_GOLD/HEAL) is also used for built-in overhead text.
      AMHC:CreateNumberEffect(caster, gold_from_unit, 3, AMHC.MSG_GOLD, "yellow", 0)
    - Easing projectile acceleration for impact punch: The gyro homing rocket ramps speed every 0.10s tick (base + 40*i) so it accelerates into the target, then triggers a 4-layer explosion particle stack + destroy sound on impact within 50 units. The accelerating approach + multi-vpcf burst gives the hit weight/'oomph'.
      dummy:SetBaseMoveSpeed( (dummy:GetBaseMoveSpeed() + (40*i)) )
      i = i + 1
      local dist = CalcDistanceBetweenEntityOBB(dummy, dummy.target)
      if dist < 50 then ... BoomGyroRocket ... end
  AI/COMBAT:
    - Tower targeting policy modifier (closest / min-hp / exit-distance): Towers disable native autoattack (MODIFIER_PROPERTY_DISABLE_AUTOATTACK) and instead run a 0.15s interval thinker that, when AttackReady() and not IsAttacking(), runs FindUnitsInRadius over acquisition range and picks a target by the player's chosen priority: 'min_hp' (lowest health), 'exit_dist' (lowest waveSpawnID = closest to lane exit), or default (first/closest), then MoveToTargetToAttack. This is the core tower AI / focus-fire system. (scripts/vscripts/towers/modifier_attack_targeting.lua)
    - Wave enemy pathing via SetInitialWaypoint + waveSpawnID ordering: Wave creeps are spawned at UnitsSpawnPlayer<id>, tagged unit.wave_unit_player and unit.waveSpawnID = round*1000 + index, and given SetInitialWaypoint('waystart_player'<id>) so engine pathing walks them through the maze. The round system polls all units' wave_unit_player to detect 'lane cleared' and start the next/extra round. waveSpawnID doubles as the kill-order key for exit-distance targeting. (scripts/vscripts/rounds_system.lua)
    - Homing missile dummy with live re-targeting (gyro rocket): On attack start (if cooldown ready) spawns npc_gyro_custom_rocket dummy, sets it uncontrollable/no-healthbar/attack-disabled, and a 0.10s timer steers it (CustomMoveToPosition) toward dummy.target. If the target dies mid-flight it FindUnitsInRadius(EVERYWHERE) for the nearest enemy belonging to the same player and re-acquires; on arrival (<50u) it AoE damages and detonates. Cooldown scales inversely with attacker damage ((100/dmg)*base). (scripts/vscripts/abilities/units_abilities/gyro_attack_rocket/modifier_gyro_attack_rocket.lua)
    - Tracking-projectile multishot (Huskar split shot) + stacking burn DoT: Intrinsic modifier on OnAttack finds up to spears_count extra enemies via FindUnitsInRadius (range+100) and fires ProjectileManager:CreateTrackingProjectile (bDodgeable) at each, with an is_split_attack reentrancy guard so OnProjectileHit -> PerformAttack(ignore cooldown, never miss) doesn't recurse infinitely. OnAttackLanded applies a queue-based stacking burn that ticks damage = stacks*per_stack and expires stacks individually from a time-ordered queue. (scripts/vscripts/abilities/units_abilities/huskar_split_shot/huskar_split_shot.lua)
    - Summoned auto-attacking pet AI with leash timer (Lich dragon): Doctrine summons npc_dota_citadel_dragon then SearchTarget runs a 0.5s thinker: if the pet isn't already attacking, FindUnitsInRadius(search_radius) and ExecuteOrderFromTable a DOTA_UNIT_ORDER_ATTACK_TARGET on the nearest enemy. A separate 10s timer kills the pet (death smoke particle + FarmTD.LichDragonDie sound), implementing a temporary aggressive minion. (scripts/vscripts/abilities/units_abilities/doctrine_spawn_dragon.lua)
    - Activatable bosses with cooldown buff, charges, and DPR tracking: Three boss items (first/second/third_boss_activate) each spawn npc_dota_unit_first/second/third_boss at the spawn point with SetInitialWaypoint, attach modifier_calculate_dpr (damage-per-round tracking), modifier_phased_units (walk through towers), and modifier_no_vision_on_attack, plus a 200s modifier_*_boss_cooldown_buff. Item uses charges (decrement or UTIL_Remove at 0) and a mutation can zero the cooldown; a 200s timer notifies + EmitSoundOnEntityForPlayer when re-castable. (scripts/vscripts/items/first_boss_activate.lua)

## RPG 0.33 (Dota 2 custom game, Steam Workshop id 958734453)
  PARTICLES:
    - Full particle lifecycle on a moving projectile (Pudge Meat Hook): Canonical pattern for a long-lived effect: create with PATTACH_CUSTOMORIGIN owned by the caster, mark SetParticleAlwaysSimulate so it keeps updating off-screen, drive the chain via SetParticleControlEnt to a named attach point (attach_weapon_chain_rt / attach_hitloc) on the hook origin, pack scalars (speed/distance/width) into a single CP vector, use boolean flag CPs (CP4/CP5 = Vector(1,0,0)) to toggle extend/retract states from Lua, and explicitly DestroyParticle when the hook finishes. Short-lived impact FX use the create -> SetParticleControlEnt -> ReleaseParticleIndex one-liner so the engine auto-frees after the effect plays out. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_pudge\pudge_meat_hook_lua.lua)
      self.nChainParticleFXIndex = ParticleManager:CreateParticle( "particles/units/heroes/hero_pudge/pudge_meathook.vpcf", PATTACH_CUSTOMORIGIN, self:GetCaster() )
      ParticleManager:SetParticleAlwaysSimulate( self.nChainParticleFXIndex )
      ParticleManager:SetParticleControlEnt( self.nChainParticleFXIndex, 0, self:GetCaster(), PATTACH_POINT_FOLLOW, "attach_weapon_chain_rt", self:GetCaster():GetOrigin() + self.vHookOffset, true )
      ParticleManager:SetParticleControl( self.nChainParticleFXIndex, 2, Vector( self.hook_speed, self.hook_distance, self.hook_width ) )
      -- impact one-shot, auto-released:
      local nFXIndex = ParticleManager:CreateParticle( "particles/units/heroes/hero_pudge/pudge_meathook_impact.vpcf", PATTACH_CUSTOMORIGIN, hTarget )
      ParticleManager:SetParticleControlEnt( nFXIndex, 0, hTarget, PATTACH_POINT_FOLLOW, "attach_hitloc", self:GetCaster():GetOrigin(), true )
      ParticleManager:ReleaseParticleIndex( nFXIndex )
    - Managed particle wrapper with timed auto-release + callback (AMHC:CreateParticle): The custom AMHC framework wraps ParticleManager so callers never leak indices: it creates the particle, spins up a 0.01s self:Timer keyed by the particle name, and when GameTime - start >= duration it calls DestroyParticle(p, immediately) and fires an optional callback. There is also a CreateParticleForPlayer twin built on ParticleManager:CreateParticleForPlayer for client-private FX. This is the project's standard way to fire-and-forget timed effects without manual cleanup bookkeeping. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\amhc_library\amhc.lua)
      local p = ParticleManager:CreateParticle(particleName,particleAttach,owningEntity)
      local time = GameRules:GetGameTime();
      self:Timer(particleName,function()
        if (GameRules:GetGameTime()-time)>=duration then
          ParticleManager:DestroyParticle(p,immediately)
          if callback~=nil then callback() end
          return nil
        end
        return 0.01
      end,0)
      return p
    - Physics projectile rig: velocity-CP + ground reflection (projectiles.lua): Custom physics projectile library creates the vpcf with PATTACH_CUSTOMORIGIN and no owner (nil), then every frame pushes position into iPositionCP and the *scaled velocity* (vel*30) into iVelocityCP so the .vpcf's own motion/trail reacts to engine-side speed. On a ground bounce it reflects velocity with the surface normal ((-2*vel:Dot(normal)*normal)+vel) and writes the reflected vector straight into the velocity CP. SetParticleControlForward/SetParticleControlOrientation are used to keep the model facing its travel direction. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\libraries\projectiles.lua)
      projectile.id = ParticleManager:CreateParticle(projectile.EffectName, PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControl(projectile.id, projectile.iPositionCP, projectile.vSpawnOrigin)
      ParticleManager:SetParticleControlForward(projectile.id, projectile.iPositionCP, projectile.vel:Normalized())
      -- on bounce:
      ParticleManager:SetParticleControl(projectile.id, projectile.iVelocityCP, ((-2 * projectile.vel:Dot(normal) * normal) + projectile.vel) * 30)
    - Top-down 'shadow on ground' raised-camera trick (neutral autocast targeting FX): For an RPG with a raised camera, ground-target FX are created on PATTACH_WORLDORIGIN and the three position CPs are set to the ground point with one CP pushed to z+1000 (the comment literally says raise this if you increase camera height). When the marker is on a unit it uses GetBoundingMaxs().z to put the FX at the top of the model. Useful concrete technique for any non-default camera height. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\ai_neutral_autocast.lua)
      local particle = ParticleManager:CreateParticle(keys.particle, PATTACH_WORLDORIGIN, target)
      ParticleManager:SetParticleControl(particle, 0, Vector(target:GetAbsOrigin().x,target:GetAbsOrigin().y,target:GetAbsOrigin().z + target:GetBoundingMaxs().z ))
      ParticleManager:SetParticleControl(particle, 1, Vector(target:GetAbsOrigin().x,target:GetAbsOrigin().y,1000 ))
    - Beam-between-two-units wiring (Luna Eclipse): Classic beam pattern: attach the particle to the caster with PATTACH_ABSORIGIN_FOLLOW, then bind CP0 to the caster and CP1/CP5 to the target via SetParticleControlEnt with the 'follow_origin' attach point so the beam endpoints track both units as they move. Paired with EmitSoundOn the target per impact for a per-hit audio cue inside a multi-target loop. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_luna\eclipse.lua)
      beam = ParticleManager:CreateParticle("particles/units/heroes/hero_luna/luna_eclipse_impact.vpcf", PATTACH_ABSORIGIN_FOLLOW, caster)
      ParticleManager:SetParticleControlEnt(beam, 0, caster, PATTACH_ABSORIGIN_FOLLOW, "follow_origin", caster:GetAbsOrigin(), true )
      ParticleManager:SetParticleControlEnt(beam, 1, target, PATTACH_ABSORIGIN_FOLLOW, "follow_origin", target:GetAbsOrigin(), true )
      EmitSoundOn("Hero_Luna.Eclipse.Target", target)
    - Particle props attached to units via Attachments library: Attachments:AttachProp spawns a prop entity at a unit's attach point and can also bind a particle to that prop (CreateParticle PATTACH_ABSORIGIN, then SetParticleControlEnt per CP to a follow point), driven by a persistent Timers loop that keeps the prop/particle glued to the unit. This is how cosmetic/equipment FX (mounts, pets, wings) are wired in the RPG, with save/load of attach offsets to KV. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\libraries\attachments.lua)
      prop.fx = ParticleManager:CreateParticle(particleName, PATTACH_ABSORIGIN, prop)
      ParticleManager:SetParticleControlEnt(prop.fx, tonumber(k), prop, PATTACH_POINT_FOLLOW, ent_point, prop:GetAbsOrigin(), true)
  SOUNDS:
    - EmitSoundOn / StartSoundEvent / StopSoundEvent: Abilities emit named .vsndevts soundevents directly on units. Pudge hook uses a sequence (AttackHookExtend on cast, AttackHookImpact on the victim, AttackHookRetract / AttackHookRetractStop on pull-back) so the audio mirrors the state machine of the hook. Windrunner Powershot shows the continuous-channel idiom: StopSoundEvent on the windup loop, then StartSoundEvent for the release loop, and a per-victim StartSoundEvent on each unit damaged during traversal. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_windrunner\powershot.lua)
    - Hero-vs-creep sound branching: Lich Chain Frost picks the impact soundevent by victim type: unit:EmitSound("Hero_Lich.ChainFrostImpact.Hero") for real heroes vs ".Creep" otherwise, giving the beefier hit sound only against players/bosses. Simple, reusable game-feel trick for making big targets feel impactful. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_lich\chain_frost.lua)
    - Fire-sound-at-a-dummy then ForceKill: Crystal Maiden Freezing Field spawns a throwaway npc_dummy_unit at the random explosion point, applies a ref modifier, StartSoundEvent on the dummy so the blast sound originates at the world position (not the caster), then a 0.1s Timer ForceKills the dummy. This is the standard way to play a positional 3D sound at an arbitrary point that has no persistent entity. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_crystal_maiden\freezing_field.lua)
    - Client-targeted UI/economy sounds (EmitSoundOnClient): The Containers inventory library plays sounds only to the relevant player using EmitSoundOnClient("General.Sell", player) alongside SendOverheadEventMessage(OVERHEAD_ALERT_GOLD,...) when an item is sold, so the buy/sell feedback is private to that player rather than broadcast. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\libraries\containers.lua)
    - Panorama UI sounds via Game.EmitSound: The main HUD JS plays Valve built-in UI sound aliases on panel toggle: Game.EmitSound("ui_chat_slide_out") when opening a panel and Game.EmitSound("ui.profile_close") when closing it. This is the panorama-side equivalent of EmitSound and the only client-script audio in the addon (no CSS animation-name driven sounds were used). (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\panorama\scripts\custom_game\mainhud.js)
    - Pitch/volume-shaped sound via EmitSoundParams: AMHC:GivePlayerGold plays the sell jingle with explicit pitch+volume control: hero:EmitSoundParams("General.Sell",200,200,1) (pitch 200, volume 200) so the reward chime is punchier than the default. Shows the project shaping stock soundevents rather than authoring new ones. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\amhc_library\amhc.lua)
  GAME-FEEL:
    - Floating combat-text / damage numbers (AMHC:CreateNumberEffect): The RPG's entire floating-number system is a particle (particles/msg_fx/msg_damage.vpcf, msg_crit, msg_heal, msg_gold, msg_evade, msg_miss, etc.) attached at attach_hitloc. CP1 packs a control vector Vector(10, number, icon_type), CP2 = Vector(duration, digit_count, 0), and CP3 = the RGB color, so one .vpcf renders arbitrary numbers/colors/icons. Built on the managed CreateParticle so it self-destroys after duration. This is the core feedback layer for an RPG (white damage, yellow crits, green heals, MISS/EVADE popups).
      local particle = AMHC:CreateParticle(msg_type,PATTACH_CUSTOMORIGIN_FOLLOW,false,entity,duration)
      ParticleManager:SetParticleControlEnt(particle,0,entity,5,"attach_hitloc",entity:GetOrigin(),true)
      ParticleManager:SetParticleControl(particle,1,Vector(10,number,icon_type))
      ParticleManager:SetParticleControl(particle,2,Vector(duration,number_count,0))
      ParticleManager:SetParticleControl(particle,3,color_vec)
    - Temporary model squash/grow for hit impact (AMHC:AddModelScale): Game-feel helper that animates a unit's model scale up to unit_scale*scale over time via a 0.03-step Timer (and caches the original scale so it can return), giving an enlarge/'pop' effect on buffs or boss-phase transitions. The juice equivalent of a squash-and-stretch since Dota has no built-in time-scale/hitstop.
      unit._amhc_AddModelScale_min_scale = unit:GetModelScale()
      local maxScale = unit_scale*scale
      local speed = 0.03
      AMHC:Timer("AddModelScale",function()
        -- grows model toward maxScale each tick
      end,0)
    - Cinematic camera lock with auto-release (AMHC:SetCamera): Camera control for cutscene/teleport/boss-intro moments: PlayerResource:SetCameraTarget(playerid, entity) snaps the player camera onto an entity, then a short Timer calls SetCameraTarget(playerid, nil) to hand control back. Dota's only sanctioned camera-takeover hook, used here as a one-shot focus pull.
      PlayerResource:SetCameraTarget(playerid,entity)
      self:Timer("SetCamera",function()
        PlayerResource:SetCameraTarget(playerid,nil)
        return nil
      end,0.01)
    - Overhead alert popups for economy/feedback: SendOverheadEventMessage(player, OVERHEAD_ALERT_GOLD, unit, cost, player) drives Valve's built-in floating gold/alert text on sell/buy in the Containers shop, complementing the custom msg_fx particle numbers for in-world feedback.
      SendOverheadEventMessage(player, OVERHEAD_ALERT_GOLD, unit, cost, player)
      EmitSoundOnClient("General.Sell", player)
    - Per-hit reactive audio/visual state machine (Powershot traversal): Powershot fires StopSoundEvent(windup) -> StartSoundEvent(travel), then in a 1/30s Timer applies damage, fires StartSoundEvent("Hero_Windrunner.PowershotDamage") on each newly-hit unit, destroys trees, and pushes a fresh FOW viewer along the path. The escalating per-unit damage falloff plus per-hit sound makes the skillshot read as physically punching through a line of enemies.
      ApplyDamage( damageTable )
      ability.powershot_damage_percent = ability.powershot_damage_percent * ( 1.0 - ability.powershot_damage_reduction )
      ability.powershot_percent_movespeed = ability.powershot_percent_movespeed * ( 1.0 - ability.powershot_speed_reduction )
      StartSoundEvent( "Hero_Windrunner.PowershotDamage", v )
    - Note: no native ScreenShake / hitstop / vignette: Across all vscripts there is no ScreenShake, time-scale, or vignette/flash call; Dota custom games can't slow game time, so the project fakes impact with the techniques above (model-scale pops, floating crit numbers, hero-vs-creep sound branching, camera pulls). Worth knowing as a constraint when porting 'juice' ideas to this engine.
  AI/COMBAT:
    - Neutral creep think-loop AI (aggro, leash, ability autocast, ally-aggro): Complete creep/camp AI on a SetContextThink loop. Caches spawn pos + acquisition range; widens search radius to range*1.5 once aggro'd; leashes home via RetreatHome() when it strays past search_radius; FindUnitsInRadius(FIND_CLOSEST) for the nearest enemy; classifies each ability's behavior with bit.band on GetBehavior() (passive/target/no_target/point) and autocasts the right order type via ExecuteOrderFromTable; when not aggro'd it propagates aggro to nearby allies so the whole camp wakes up together. Returns think intervals (0.5/1/3) to throttle CPU. This is the reusable boss/camp AI skeleton. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\ai_neutral_autocast.lua)
    - Custom physics projectile system with sub-frame collision: Projectiles library runs projectiles on a frame timer that does sub-frame lookahead collision (tot = ceil(velLength/32) sample points along the velocity each frame) to prevent fast projectiles tunneling through units, supports bGroundLock (snap z to GetGroundPosition+offset), fExpireTime/fDistance limits, per-projectile filter and OnFinish callbacks (pcall-guarded), and ground-bounce reflection. Foundation for bespoke projectile patterns (arcs, bouncing, gravity) beyond ProjectileManager. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\libraries\projectiles.lua)
    - Bouncing tracking-projectile chain (Chain Frost / Paralyzing Cask): Bounce pattern: a counter is stored on the ability (jump_counter / bounces_left), and on each impact it FindUnitsInRadius around the current victim, picks the first unit that isn't the previous target, and spawns the next ProjectileManager:CreateTrackingProjectile from that victim (Source = current target) after a bounce_delay Timer. Cask varies stun/damage by IsHero(). Clean template for chain-lightning / ricochet boss attacks. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_witch_doctor\paralyzing_cask.lua)
    - Multi-projectile fan / split-shot targeting: Split Shot demonstrates an aimed multi-target volley: FindUnitsInRadius(FIND_CLOSEST) around the caster, then loop spawning a tracking projectile at each unit that isn't the primary attack target, decrementing max_targets and breaking at zero. The on-hit handler applies caster:GetAttackDamage() as physical damage. Reusable for boss 'spray' attacks and AoE auto-attack mechanics. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_medusa\split_shot.lua)
    - Positional combat passive (Desolate isolation check): On-attack passive that FindUnitsInRadius around the target and only triggers bonus true-attack damage if the target is isolated (no friendly units other than itself nearby) and not magic immune. Pattern for 'execute when alone / flanking' boss and rogue mechanics; also a good example of GetAverageTrueAttackDamage-based bonus damage scaling. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_spectre\desolate.lua)
    - Stacking on-hit ramp (Fury Swipes) for tank/boss DPS scaling: On-attack handler that ramps damage by reading GetModifierStackCount, dealing damage_per_stack * current_stack, then refreshing the datadriven modifier duration and SetModifierStackCount+1 (with a special longer reset window for a configurable 'exception' unit i.e. Roshan/boss). Template for boss enrage / sustained-DPS reward mechanics that need a stack-based aggro/damage curve. (C:\Users\work\.dota2-workshop-mcp\reflib\items\958734453\files\scripts\vscripts\heroes\hero_ursa\fury_swipes.lua)

## Pudge Wars 7.29 (Workshop ID 296831818)
  PARTICLES:
    - Hand-rolled chain-link particle system (ParticleUnit): The signature meat-hook chain is NOT one stock pudge_meathook particle. They spawn a chain of dummy 'hook' units plus a custom ParticleUnit class that wraps a particle index and drives three control points: cpStart (default CP0), cpEnd (default CP6) and cpDelete (default CP10). Moving the chain = SetParticleControl on cpStart/cpEnd each tick; 'destroying' a link writes Vector(0,0,0) to cpDelete instead of releasing, letting the .vpcf retract itself. New links are appended dynamically when the gap between hero and the last link exceeds linkCreationTolerance, giving an elastic rope. particles/pw/ref_pudge_meathook_chain.vpcf is the custom chain asset. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\hook.lua)
      function ParticleUnit.new(particle,position,endPosition,cpStart,cpEnd,cpDelete)
        o.cpStart=cpStart or 0; o.cpEnd=cpEnd or 6; o.cpDelete=cpDelete or 10
        ParticleManager:SetParticleControl(o.particle,o.cpStart,position)
        ParticleManager:SetParticleControl(o.particle,o.cpEnd,endPosition)
      end
      function ParticleUnit:Destroy()
        ParticleManager:SetParticleControl(self.particle,self.cpDelete,Vector(0,0,0))
      end
    - Hook-streak counter packed into one control point: Cosmetic 'successful hooks' streak: a single arcana streak particle (red vs default arcana chosen by hero.pudge_arcana) is attached PATTACH_OVERHEAD_FOLLOW and the running total is encoded into ONE control-point vector CP2 = (tens, ones, total). The .vpcf reads those CP components to render the number. Fire-and-forget: created then ReleaseParticleIndex immediately so the engine owns lifetime. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\hook.lua)
      local hook_counter=ParticleManager:CreateParticle(pfx,PATTACH_OVERHEAD_FOLLOW,hero)
      local stack_10=math.floor(hero.successful_hooks/10)
      ParticleManager:SetParticleControl(hook_counter,2,Vector(stack_10,hero.successful_hooks-stack_10*10,hero.successful_hooks))
      ParticleManager:ReleaseParticleIndex(hook_counter)
    - Impact FX attached to the exact hit-location bone: Cleaver/hook impacts use PATTACH_CUSTOMORIGIN + SetParticleControlEnt with the 'attach_hitloc' attachment and PATTACH_POINT_FOLLOW so the burst spawns on the victim's actual hit bone, then release immediately. Reused for the cleaver projectile and lifesteal/lantern bash effects. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\abilities\pudge_wars_cleaver.lua)
      local nFXIndex=ParticleManager:CreateParticle("particles/units/heroes/hero_pudge/pudge_meathook_impact.vpcf",PATTACH_CUSTOMORIGIN,hTarget)
      ParticleManager:SetParticleControlEnt(nFXIndex,0,hTarget,PATTACH_POINT_FOLLOW,"attach_hitloc",self:GetCaster():GetAbsOrigin(),true)
      ParticleManager:ReleaseParticleIndex(nFXIndex)
    - Team-colored blink FX via CreateParticleForTeam + ProjectileDodge: Blink uses CreateParticleForTeam so the depart and arrival bursts are only drawn for the owning team (vision-correct), released right after. ProjectileManager:ProjectileDodge(self) is then called to dodge incoming linear projectiles on blink — a neat reuse of the projectile API as an evade. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\internal\util.lua)
      local blink_pfx=ParticleManager:CreateParticleForTeam(blink_effect,PATTACH_ABSORIGIN,self,self:GetTeamNumber())
      ParticleManager:ReleaseParticleIndex(blink_pfx)
      local blink_end_pfx=ParticleManager:CreateParticleForTeam(blink_effect_end,PATTACH_ABSORIGIN,self,self:GetTeamNumber())
      ProjectileManager:ProjectileDodge(self)
    - Persistent looping aura particle stored on modifier, cleaned in OnDestroy: Campfire modifier keeps its particle index as self.nFXIndex created in OnCreated and explicitly ReleaseParticleIndex'd in OnDestroy — the correct pattern for a continuous aura (vs fire-and-forget). CP2 positions the flame at an offset from the caster. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\components\modifiers\modifier_campfire.lua)
      self.nFXIndex=ParticleManager:CreateParticle("particles/act_2/campfire_flame.vpcf",PATTACH_ABSORIGIN,self:GetCaster())
      ParticleManager:SetParticleControl(self.nFXIndex,2,self:GetCaster():GetOrigin()+self.vOffset)
      -- OnDestroy: ParticleManager:ReleaseParticleIndex(self.nFXIndex)
    - Timed rune effect with rope-follow CP + DestroyParticle teardown: The Spooky rune spawns death_tombstone_alt.vpcf, sets CP0/1/2 to the tomb location, then a 0.1s repeating timer updates CP10 to the laser unit's head each tick to keep a 'rope' attached; on expiry it uses ParticleManager:DestroyParticle(id,false) (immediate teardown, not release) plus StopSoundEvent — a full create/track/teardown lifecycle for a timed effect. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\runes.lua)
      local spookyId=ParticleManager:CreateParticle("particles/doto/generic_hero_status/death_tombstone_alt.vpcf",PATTACH_ABSORIGIN,caster)
      -- per-tick: ParticleManager:SetParticleControl(spookyId,10,spookyUnit:GetAbsOrigin()+Vector(0,0,250))
      -- end: ParticleManager:DestroyParticle(spookyId,false)
  SOUNDS:
    - Borrowed stock hero soundevents for hook impact/extend: Rather than authoring many sounds they reuse Valve's hero events: hero:EmitSound('Hero_Pudge.AttackHookExtend') on launch, victim:EmitSound('Hero_Pudge.AttackHookImpact') on connect, Hero_Zuus.LightningBolt on lightning hits, and EmitSoundOnLocationWithCaster(...,'Hero_Pudge.HookDrag.Arcana',hero) for the arcana drag. Wired via 'soundfile' keys pointing at game_sounds_pudge.vsndevts in npc_abilities_custom.txt. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\hook.lua)
    - Custom global 'HEADSHOT' stinger via EmitGlobalSound: The skill-shot payoff (hooking an already-hooked enemy = headshot, or a friendly = deny) fires EmitGlobalSound('Pudgewars.Headshot') — a custom event in game_sounds_custom.vsndevts (referenced from npc_units_custom.txt) played to everyone, paired with screen shake and a Notifications banner. Same sound reused for DENY with a code comment noting it should become a separate deny sound. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\hook.lua)
    - Trap sounds: positional EmitSoundOn + layered global stingers: Conquest trap pack: spike trap plays 'Conquest.SpikeTrap.Plate' on the ability entity then 'Conquest.SpikeTrap.Activate.Generic' at the location; pendulum stacks three global cues at once (EmitGlobalSound 'Conquest.Pendulum.Trigger','tutorial_rockslide','Conquest.Pendulum.Scrape') for a layered crash plus per-target 'Conquest.Pendulum.Target'. Breathe fire/poison emit 'Conquest.FireTrap.Generic'/'Conquest.PoisonTrap.Generic' on the caster. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\traps\pendulum_trap_trigger.lua)
    - Looping laser managed with StartSoundEvent/StopSoundEvent: The Spooky rune's continuous laser uses StartSoundEvent('Hero_Phoenix.SunRay.Loop',spookyUnit) at start and the matching StopSoundEvent on the same unit when the 8s effect ends — the correct loop start/stop pairing on a specific entity rather than EmitSound, which can't be stopped cleanly. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\runes.lua)
    - Panorama UI sounds: rarity-keyed loot stingers + lock-in: The end screen plays a battlepass-rarity-keyed stinger from JS: a sound_name map (common->Loot_Drop_Sfx, rare->Loot_Drop_Stinger_Rare ... arcana->Loot_Drop_Stinger_Arcana) then Game.EmitSound(sound_name[rarity]). The tutorial fires Game.EmitSound('ui_team_select_lock_and_start') on confirm. UI audio is driven from Panorama, not Lua. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\panorama\scripts\custom_game\frostrose_end_screen.js)
    - Soundevents declared per-unit/ability via 'soundfile' KV keys: Units/abilities load sound banks through 'soundfile' entries (game_sounds_pudge.vsndevts, game_sounds_custom.vsndevts, game_sounds_stingers_diretide.vsndevts, plus borrowed phoenix/venomancer/alchemist/spirit_breaker banks) so EmitSound names resolve at runtime — this is how Pudgewars.Headshot and the Conquest.* events become available. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\npc\npc_units_custom.txt)
  GAME-FEEL:
    - Screen shake on headshot/deny: The big payoff moment (headshot or deny) triggers a hard ScreenShake at the victim's origin with large amplitude/frequency and a huge radius, layered with the culling-blade kill burst + global stinger + banner. The extreme args (amplitude 100, freq 100, duration 1, radius 9999, command 0, bAirShake true) make it effectively a global jolt felt by all players.
      ScreenShake( v:GetOrigin(), 100, 100, 1, 9999, 0, true)
    - Camera distance override (arena zoom hook): On gamemode capture they call SetCameraDistanceOverride(1134.0); the code comment notes 1134 is the engine default, so the hook is in place to tune the arena zoom even though this build leaves it at default.
      GameMode:SetCameraDistanceOverride( 1134.0 )  -- Usual is 1134
    - Forced camera takeover and restore (SetCameraTarget): PlayerResource:SetCameraTarget(playerID,hTarget) locks a player's camera onto a target (taunt/spectate flow) and SetCameraTarget(playerID,nil) releases it back — manual camera takeover/restore, a strong lever for forcing the player's eye to the action.
      PlayerResource:SetCameraTarget(self:GetPlayerID(), hTarget)
      PlayerResource:SetCameraTarget(self:GetPlayerID(), nil) -- restore
    - Hook reel-in = continuous SetAbsOrigin pull (scripted knockback): The pull is pure scripted motion, not engine force: every think tick the hooked unit is teleported to the hook head and the chain links each lerp toward the previous link; on release FindClearSpaceForUnit avoids getting stuck. The hook head advances at speed/30 per 1/30s tick.
      hooked:SetAbsOrigin(hooks[1]:GetAbsOrigin() + Vector(0,0,-125))
      hooks[#hooks]:SetAbsOrigin(hooks[#hooks]:GetAbsOrigin() + direction * speed / 30)
      FindClearSpaceForUnit(hooked, hooked:GetAbsOrigin(), true)
    - Floating text / banner feedback layer: Feedback is text-driven: headshots/denies/runes push Notifications:TopToAll{text=...,duration,style={color=...}} banners (HEADSHOT!, DENIED!, FIRE RUNE! in themed colors); per-unit floats use SendOverheadEventMessage(nil,OVERHEAD_ALERT_HEAL,parent,heal,nil) for lifesteal numbers. The Notifications lib is the primary event readout.
      Notifications:TopToAll({text="HEADSHOT!", duration=4.0})
      SendOverheadEventMessage(nil, OVERHEAD_ALERT_HEAL, parent, heal, nil)
    - Custom 0.01s physics think loop for motion simulation: A bespoke Physics singleton runs a worldspawn SetContextThink at PHYSICS_THINK=0.01 with reflect/block groups and projectiles, computing dt from GameRules:GetGameTime() (noting the engine passes wall-clock not sim time) and running pcall'd timer callbacks — the substrate for sliding/bouncing knockback (PHYSICS_NAV_SLIDE/BOUNCE constants).
      PHYSICS_THINK = 0.01
      wspawn:SetContextThink("PhysicsThink", Dynamic_Wrap(Physics,'Think'), PHYSICS_THINK)
  AI/COMBAT:
    - Meat-hook: dummy-unit linear pull with per-tick collision scan: The core hook is NOT a ProjectileManager projectile. A real 'npc_reflex_hook_test' dummy unit is spawned and advanced by SetAbsOrigin each 1/30s tick; collision is detected by iterating nearby entities and classname/unitname string-matching (string.find(class,'pudge'), 'barrier' totems, 'rune'/'mine' creeps). First valid enemy sets hooked, flips complete=true, and the reel-in phase begins. Handles bounce off shields/totems, headshot (re-hook of an already-hooked enemy) and deny (re-hook of a friendly). (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\hook.lua)
    - Cleaver: ProjectileManager linear projectile + OnProjectileHit: pudge_wars_cleaver builds a CreateLinearProjectile info table (vVelocity from cursor dir * projectile_speed, fDistance=range, start/end radius, bDeleteOnHit, magic-immune-piercing flag, provides vision) and in OnProjectileHit applies magic damage + a stacking move-slow modifier, hiding the offhand weapon wearable during flight and restoring it on miss/end. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\abilities\pudge_wars_cleaver.lua)
    - Breathe-fire trap: expanding linear projectile (Dragon Knight pattern): Trap fire breath is a linear projectile with start_radius->end_radius expansion; speed is pre-scaled by range/(range-start_radius) so the cone visual lines up with the hit window. OnProjectileHit applies magical damage scaled by targetLevel/3, crediting caster.KillerToCredit when set. Same template reused for breathe_poison. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\traps\breathe_fire.lua)
    - Spike trap AI: trigger-gated, delayed AoE think with re-arm latch: Map trap driven by entity triggers: Fire(trigger) reads the activator, guards with a triggerActive latch, plays animation/particles via DoEntFire Start/Stop pairs, then Timers:CreateTimer(delay) runs the AoE — DestroyTreesAroundPoint + FindUnitsInRadius (BOTH teams, hero+basic) applying physical damage + modifier_stunned, and SetContextThink('ResetTrapModel',...,4) re-arms the latch after 4s. The reusable trap-AI shape. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\traps\spike_trap_ai.lua)
    - Pendulum trap: instant-kill AI with credit attribution + phase-dodge: Pendulum Fire(trigger) resolves the hero from the activator's EntIndexToHScript, early-outs if IsOutOfGame (phase-shift dodge), spawns a lifestealer blood-emerge particle on attach_hitloc (stored via Attribute_SetIntValue 'effectsID'), then heroHandle:Kill(nil,killerEntity) using thisEntity.KillerToCredit so the kill is credited to the player who armed/triggered it. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\traps\pendulum_trap_ai.lua)
    - Lightning-rune combo: chained disposable-caster spell AI: When a hooked target has modifier_disruptor_thunder_strike, the code spawns npc_lightning_dummy + npc_arc_dummy (team chosen so arcs jump to the right side), then sequences castInstantSpell(pudge_wars_lightning_field) and castSpell(pudge_wars_arc_lightning)/castSpell(pudge_wars_lightning_bolt) on the dummies to produce a multi-spell lightning burst on the victim — emergent ability AI built from disposable caster units. (C:\Users\work\.dota2-workshop-mcp\reflib\items\296831818\files\scripts\vscripts\hook.lua)

## Skillshot Wars (id 297809214) — a team-deathmatch skillshot PvP arena. First team to 50 hero kills wins (winKills=50 in addon_game_mode.lua). Every player auto-gets the same skillshot kit on spawn (Pudge Meat Hook, Mirana Sacred Arrow, Invoker Sun Strike, Clockwerk Hookshot) via UpgradeAbility in OnHeroPicked. NOTE: there is NO creep/tower/boss AI in this game — combat is 100% player-driven skillshots. A gnoll_assassin creature is defined in npc_units_custom.txt but is never spawned by any script.
  PARTICLES:
    - Persistent chain particle stored on the ability + control-point protocol (Meat Hook): The hook's chain VPCF index is cached on the ability object (self.nChainParticleFXIndex) so it survives across OnSpellStart -> OnProjectileHit -> destroy. CP0 = origin end (followed to caster chain attach), CP1 = far end / hooked victim, CP2 packs (speed, distance, width) as a Vector, CP3 a 'killswitch' timeout vector, CP4/CP5 are 0/1 toggle vectors that flip extend vs retract state inside the GPU sim. SetParticleAlwaysSimulate keeps it ticking off-screen. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\meat_hook_skill.lua)
      self.nChainParticleFXIndex = ParticleManager:CreateParticle( "particles/units/heroes/hero_pudge/pudge_meathook.vpcf", PATTACH_CUSTOMORIGIN, self:GetCaster() )
      ParticleManager:SetParticleAlwaysSimulate( self.nChainParticleFXIndex )
      ParticleManager:SetParticleControlEnt( self.nChainParticleFXIndex, 0, self:GetCaster(), PATTACH_POINT_FOLLOW, "attach_weapon_chain_rt", self:GetCaster():GetOrigin() + self.vHookOffset, true )
      ParticleManager:SetParticleControl( self.nChainParticleFXIndex, 2, Vector( self.hook_speed, self.hook_distance, self.hook_width ) )
    - Re-parenting the chain CP from caster-weapon to victim hitloc on impact: On a successful hook the same persistent particle is mutated, not recreated: CP1 is re-attached from the caster's chain bone to the victim's 'attach_hitloc' via PATTACH_POINT_FOLLOW, and the 0/1 toggle CPs (4 and 5) are swapped so the VPCF switches from extend to retract visuals while dragging the body back. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\meat_hook_skill.lua)
      ParticleManager:SetParticleControlEnt( self.nChainParticleFXIndex, 1, hTarget, PATTACH_POINT_FOLLOW, "attach_hitloc", hTarget:GetOrigin() + self.vHookOffset, true )
      ParticleManager:SetParticleControl( self.nChainParticleFXIndex, 4, Vector( 0, 0, 0 ) )
      ParticleManager:SetParticleControl( self.nChainParticleFXIndex, 5, Vector( 1, 0, 0 ) )
    - Cleanup: DestroyParticle(true) for the persistent FX, ReleaseParticleIndex for one-shots: Two distinct cleanup idioms. The long-lived chain particle is force-killed with DestroyParticle(idx, true) (the bool = immediate) once the projectile returns. The one-shot impact burst is fire-and-forget: created, set, then handed back with ReleaseParticleIndex so the engine GCs it after its endcap finishes. Mixing these correctly is the key leak-avoidance technique. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\meat_hook_skill.lua)
      local nFXIndex = ParticleManager:CreateParticle( "particles/units/heroes/hero_pudge/pudge_meathook_impact.vpcf", PATTACH_CUSTOMORIGIN, hTarget )
      ParticleManager:SetParticleControlEnt( nFXIndex, 0, hTarget, PATTACH_POINT_FOLLOW, "attach_hitloc", self:GetCaster():GetOrigin(), true )
      ParticleManager:ReleaseParticleIndex( nFXIndex )
      -- ...later, on retract finish:
      ParticleManager:DestroyParticle( self.nChainParticleFXIndex, true )
    - Per-player team-only charge particle via CreateParticleForPlayer (Sun Strike telegraph): The sun-strike charge-up ring is only shown to the caster's team: it loops over HeroList:GetAllHeroes(), filters by team, and spawns a per-player particle with CreateParticleForPlayer so enemies don't see the warning. CP0 = target point, CP1 = AoE radius packed in a Vector. The particle is torn down with a Timers:CreateTimer at the strike delay, not via the engine, so the telegraph disappears exactly when the strike lands. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\sun_strike_skill.lua)
      local particle = ParticleManager:CreateParticleForPlayer(charge_particle, PATTACH_ABSORIGIN, hero, PlayerResource:GetPlayer(hero:GetPlayerID()))
      ParticleManager:SetParticleControl(particle, 0, target_location)
      ParticleManager:SetParticleControl(particle, 1, Vector(area_of_effect,0,0))
      Timers:CreateTimer(delay, function()
        ParticleManager:DestroyParticle(particle, false)
      end)
    - Data-driven FireEffect with inline ControlPoints (KV, no Lua): The Sun Strike impact and AoE ring are driven purely from npc_abilities_custom.txt using ability_datadriven FireEffect blocks. EffectAttachType follow_origin binds CP0 to the target, and a nested ControlPoints block sets CP1 to the AoE radius as a string vector '%area_of_effect 0 0'. This is the no-script way to wire particle control points alongside FireSound and DelayedAction. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\npc\npc_abilities_custom.txt)
      "FireEffect"
      {
        "EffectName"        "particles/units/heroes/hero_invoker/invoker_sun_strike.vpcf"
        "EffectAttachType"  "follow_origin"
        "Target"            "TARGET"
        "ControlPoints" { "01" "%area_of_effect 0 0" }
      }
    - Particle precaching strategy (wildcard sound + per-ability VPCF): Two-tier precache. The game mode bulk-precaches all sound with the wildcard PrecacheResource('soundfile','*.vsndevts') and precaches each hero unit (which pulls their stock particles). Each datadriven ability additionally lists its specific VPCFs in a precache{} block (sun strike main + team variant, mirana arrow). The Lua-based hook relies on the precached pudge unit for its meathook/impact VPCFs. Panorama has NO particle scene panels - UI is plain (top_scoreboard.xml just includes a CSS file). (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\addon_game_mode.lua)
      PrecacheResource( "soundfile", "*.vsndevts", context )
      PrecacheUnitByNameSync("npc_dota_hero_pudge",context)
      -- in ability KV:
      "precache" {
        "particle" "particles/units/heroes/hero_invoker/invoker_sun_strike.vpcf"
        "particle" "particles/units/heroes/hero_invoker/invoker_sun_strike_team.vpcf"
      }
  SOUNDS:
    - Full hook audio lifecycle via EmitSoundOn at each state transition: The Lua hook fires four stock Pudge sound events, each EmitSoundOn'd at a precise phase: Hero_Pudge.AttackHookExtend on cast (on caster), AttackHookImpact when the hook grabs (on victim), AttackHookRetract while dragging back (on victim), and AttackHookRetractStop when it returns empty (on caster). Tying sounds to the same state machine that drives the particle CP toggles gives the hook its weighty extend/snap/reel feel. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\meat_hook_skill.lua)
    - Data-driven FireSound chained to charge/ignite phases (Sun Strike): Sun Strike uses KV FireSound blocks rather than Lua: Hero_Invoker.SunStrike.Charge fires in OnCreated of the delay modifier, and Hero_Invoker.SunStrike.Ignite fires inside the DelayedAction at the strike. A clever trick: the target is made modifier_invisible for 'delay - 0.1' specifically 'so that the charge sound isnt heard' by the victim - audio is used as an anti-tell stealth mechanic. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\npc\npc_abilities_custom.txt)
    - Mirana arrow cast/impact sounds wired to projectile callbacks (data-driven): Hero_Mirana.ArrowCast fires on the caster in OnSpellStart, and Hero_Mirana.ArrowImpact fires on the target in OnProjectileHitUnit, alongside the RunScript that computes distance-scaled stun. Demonstrates pairing FireSound with LinearProjectile + OnProjectileHitUnit in pure KV. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\npc\npc_abilities_custom.txt)
    - Per-hero GameSoundsFile / SoundSet on custom creature + soundfile precache in ability KV: The custom creature declares its own SoundSet 'n_creep_Ranged' and GameSoundsFile pointing at game_sounds_creeps.vsndevts. Abilities reference stock soundevent files (game_sounds_invoker.vsndevts, game_sounds_mirana.vsndevts) in their precache blocks so the stock EffectName strings resolve. No custom .vsndevts of their own are authored - they reuse Valve's. No sound-on-UI or CSS animation-name sound is used (panorama is minimal). (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\npc\npc_units_custom.txt)
  GAME-FEEL:
    - Horizontal motion controller drags the victim every frame (hook pull): Knockback/pull is implemented with ApplyHorizontalMotionController in the debuff modifier's OnCreated; if it returns false (target already controlled) the modifier self-destructs. UpdateHorizontalMotion then teleports the victim to the projectile's tracked location each tick (self.vProjectileLocation, updated in OnProjectileThink). This is the canonical Dota motion-controller technique for forced movement, with a built-in conflict guard.
      function modifier_meat_hook_skill:OnCreated( kv )
        if IsServer() then
          if self:ApplyHorizontalMotionController() == false then self:Destroy() end
        end
      end
      function modifier_meat_hook_skill:UpdateHorizontalMotion( me, dt )
        self:GetAbility().hVictim:SetOrigin( self:GetAbility().vProjectileLocation )
      end
    - Gesture override + flail animation sells the hooked-and-dragged feel: The caster plays ACT_DOTA_OVERRIDE_ABILITY_1 during cast (in OnAbilityPhaseStart) then swaps to ACT_DOTA_CHANNEL_ABILITY_1 on the retract, removing/restarting gestures on interrupt and death. The victim modifier returns MODIFIER_PROPERTY_OVERRIDE_ANIMATION -> ACT_DOTA_FLAIL so the dragged unit visibly flails. Animation state mirrors the combat state machine.
      function modifier_meat_hook_skill:GetOverrideAnimation( params )
        return ACT_DOTA_FLAIL
      end
      -- caster: StartGesture( ACT_DOTA_OVERRIDE_ABILITY_1 ) -> RemoveGesture(...) -> StartGesture( ACT_DOTA_CHANNEL_ABILITY_1 )
    - Distance-scaled stun + damage falloff (skill-rewarding hit feel): Mirana's arrow computes travel distance (Length2D of impact vs launch origin captured in LaunchArrow) and maps it through piecewise stun curves: a per-30-units ramp up to max range, a flat plateau, then a linear falloff slope, bottoming out at min stun. Longer skillshots = bigger reward (up to 5s stun). This distance-based tuning is the core 'game feel' lever for the whole title.
      local distance = (target_location - arrowTable[caster].location):Length2D()
      if distance < arrow_max_stunrange then
        arrow_stun_duration = distance*0.033*stun_per_30 + arrow_min_stun
      elseif distance < arrow_min_falloff_stunrange then
        arrow_stun_duration = arrow_max_stun
      else
        arrow_stun_duration = arrow_max_stun + stun_falloff_slope * (distance - arrow_min_falloff_stunrange)
      end
    - Forced-movement followthrough self-stun on the caster: On cast the caster applies modifier_meat_hook_skill_followthrough to itself for duration = distance/speed*followthrough_constant. That hidden modifier's CheckState returns MODIFIER_STATE_STUNNED, locking the caster in place while the hook flies - a committed, high-risk cast window that defines the skillshot's give-and-take feel.
      local flFollowthroughDuration = ( self.hook_distance / self.hook_speed * self.hook_followthrough_constant )
      self:GetCaster():AddNewModifier( self:GetCaster(), self, "modifier_meat_hook_skill_followthrough", { duration = flFollowthroughDuration } )
      -- modifier: CheckState -> { [MODIFIER_STATE_STUNNED] = true }
    - Vision reveal as a tactical 'flash' on hit (FOW viewer / visibility node): Every landed skillshot briefly lights up the target area for the caster's team: the hook calls AddFOWViewer at the victim, while arrow and sun strike use ability:CreateVisibilityNode. There is no literal screen-flash/vignette/ScreenShake/hit-stop/camera control/floating-damage-text in this game; the 'reward feedback' for landing a shot is granted map vision (radius/duration are ability specials).
      AddFOWViewer( self:GetCaster():GetTeamNumber(), hTarget:GetOrigin(), self.vision_radius, self.vision_duration, false )
      -- arrow/sun strike: ability:CreateVisibilityNode(target_location, vision_radius, vision_duration)
    - Instant position-swap teleport (Vengeful Spirit Swap): Swap is a brutally simple two-line teleport: read both AbsOrigins, then SetAbsOrigin each unit to the other's spot. No interpolation, no particle, no lerp - an intentionally snappy repositioning tool. Worth noting as the minimal-viable motion ability contrasting the elaborate hook.
      local casterPos = caster:GetAbsOrigin()
      local targetPos = target:GetAbsOrigin()
      target:SetAbsOrigin(casterPos)
      caster:SetAbsOrigin(targetPos)
  AI/COMBAT:
    - Linear (non-tracking) projectile as the universal skillshot primitive: Combat is built on ProjectileManager:CreateLinearProjectile (Lua, hook) and the KV LinearProjectile block (arrow) - never tracking projectiles. The hook spawns a linear projectile with vVelocity = dir*speed, fStartRadius/fEndRadius = width, broad target flags (magic-immune enemies, not ancients, invulnerable) so it can grab anything. On hit it spawns a SECOND linear projectile aimed back at the caster to reel the body in (vSpawnOrigin=vHookPos, vVelocity = (start-hookpos):Normalized()*speed). This dual-projectile reel is the projectile 'pattern'. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\meat_hook_skill.lua)
    - OnProjectileHit state machine (extend phase vs retract phase): Targeting/aggro is entirely player-aimed; the 'AI' is the per-projectile callback. OnProjectileHit branches on self.bRetracting: first hit (extend) validates target is a creep/consideredhero, applies pure damage + Interrupt + the pull modifier modifier_meat_hook_skill, reveals vision, then launches the return projectile and flips bRetracting=true; the second call (retract) cleans up and FindClearSpaceForUnit at the start position. Self-hits ignored (hTarget==caster returns false). (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\meat_hook_skill.lua)
    - FindUnitsInRadius for AoE target acquisition + damage splitting (Sun Strike): The only radius-search 'targeting' in the game: sun_strike_damage runs FindUnitsInRadius at the strike point (team, point, nil, radius, target teams/types/flags pulled from the ability, FIND_CLOSEST, false) and splits damage evenly across everyone found (damage / #found_targets). This is the closest thing to area combat logic; there is no aggro/leash/think-loop enemy AI anywhere. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\abilities\sun_strike_skill.lua)
    - Global think loop is scorekeeping only - no unit AI: The single SetThink('OnThink',self,'GlobalThink',2) loop just polls game state; OnThink/GameThink do nothing but check DOTA_GAMERULES_STATE and (in GameThink) flush statcollection at POST_GAME. Win/score logic lives in the dota_team_kill_credit event handler (OnTeamKillCredit) which awards gold and calls GameRules:SetGameWinner when event.herokills >= winKills(50). There is no creep spawning, no tower AI, no boss phases. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\addon_game_mode.lua)
    - Kit auto-grant + economy on spawn (defines the PvP loadout, no NPC controllers): OnHeroPicked (dota_player_pick_hero) finds the four skillshot ability entities by name (meat_hook_skill, arrow_skill, sun_strike_skill, rattletrap_hookshot via FindByClassname) and UpgradeAbility's them onto the hero, zeroes ability points/death XP, and seeds 250 starting gold. This is the 'combat setup' - every hero is a skillshot platform; the game ships no AI-controlled combatants to fight. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\vscripts\addon_game_mode.lua)
    - Unused creature template (evidence of cut PvE): npc_units_custom.txt defines npc_dota_creature_gnoll_assassin (ranged neutral, ProjectileSpeed 1500, ProjectileModel particles/neutral_fx/gnoll_base_attack.vpcf, Creature{} level-up gains). No Lua ever spawns it and no ability summons it - so the shipped game has no creep/tower/boss AI despite the scaffolding. Listed so the parent agent doesn't mistake it for active AI. (C:\Users\work\.dota2-workshop-mcp\reflib\items\297809214\files\scripts\npc\npc_units_custom.txt)

## Dota Global Skillshots (id 466843568) — "Dotaȫͼ��" by Tim the hexed. Core conceit: targeted abilities are re-engineered into global-range manual skillshots. AbilityCastRange is bumped to 20000 across pudge_meat_hook / mirana_arrow / dismember / ether_shock (in npc_abilities_override.txt), and Pudge's hook is fully rewritten as a custom dummy-driven projectile (meat_hook.lua) so it can travel across the entire map. Decompiled source at C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files. Notable: it is NOT a PvE/boss game — it is a competitive 5v5 with a persistent Elo/win-streak ladder (StorageAPI), abandon-reimbursement gold rebalancing, and per-skillshot hit/miss accuracy tracking. There is no custom creep/tower/boss AI; "combat AI" here is the manual-skillshot projectile simulation and the few rewritten hero abilities.
  PARTICLES:
    - Dummy-anchored hook projectile (CP0=caster attach, CP6=moving dummy): Pudge hook particle is attached to the caster's attack hand at control point 0 and to a separately-moved invisible dummy unit at control point 6. Moving the dummy each 0.03s tick drags the visible rope endpoint, so the projectile's visual is entirely driven by repositioning an entity rather than by the .vpcf itself. Cleanup uses DestroyParticle (immediate) when the hook retracts, paired with dummy:RemoveSelf(). (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\heroes\hero_pudge\meat_hook.lua)
      local particle = ParticleManager:CreateParticle(hook_particle, PATTACH_RENDERORIGIN_FOLLOW, caster)
      ParticleManager:SetParticleControlEnt(particle, 0, caster, 5, "attach_attack1", casterLocation, false)
      ParticleManager:SetParticleControlEnt(particle, 6, dummy, 5, "attach_hitloc", dummyLocation, false)
      -- on retract finish:
      ParticleManager:DestroyParticle(particle, true)
      dummy:RemoveSelf()
    - ReleaseParticleIndex for fire-and-forget impact FX: Huskar Life Break impact effect is created, both CPs bound to the target's attach_hitloc, then immediately ReleaseParticleIndex'd. This is the standard pattern for a one-shot self-terminating effect: you do not hold the index because the .vpcf ends on its own; releasing avoids leaking the handle. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\heroes\hero_huskar\life_break.lua)
      local particle = ParticleManager:CreateParticle("particles/units/heroes/hero_huskar/huskar_life_break.vpcf", PATTACH_ABSORIGIN_FOLLOW, target)
      ParticleManager:SetParticleControlEnt(particle, 0, target, PATTACH_ABSORIGIN_FOLLOW, "attach_hitloc", target:GetAbsOrigin(), true)
      ParticleManager:SetParticleControlEnt(particle, 1, target, PATTACH_ABSORIGIN_FOLLOW, "attach_hitloc", target:GetAbsOrigin(), true)
      ParticleManager:ReleaseParticleIndex(particle)
    - Datadriven FireEffect + StatusEffectName (no Lua): Where possible, particles are wired declaratively in the ability KV instead of Lua: OnSpellStart>FireEffect spawns the cast particle with follow_origin attach on CASTER, and the debuff modifier uses StatusEffectName to overlay a persistent screen/body status FX with a StatusEffectPriority. Precaching is done in the ability's "precache" block. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\npc\npc_abilities_custom.txt)
      "FireEffect"
      {
        "EffectName"        "particles/units/heroes/hero_huskar/huskar_life_break_cast.vpcf"
        "EffectAttachType"  "follow_origin"
        "Target"            "CASTER"
      }
      // debuff modifier:
      "StatusEffectName"     "particles/status_fx/status_effect_huskar_lifebreak.vpcf"
      "StatusEffectPriority" "15"
    - AttachEffect on OnIntervalThink (periodic modifier FX): An item (Slippers of Halcyon caster) attaches a heal particle and fires a sound every think tick via the datadriven modifier's OnIntervalThink>AttachEffect block, with ThinkInterval driving the cadence. Pattern for per-tick aura/regen visuals without any Lua. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\npc\npc_items_custom.txt)
      "OnIntervalThink"
      {
        "Heal" { "Target" "TARGET" "HealAmount" "%hp_per_tick" }
        "AttachEffect"
        {
          "Target" "TARGET"
          "EffectName"     "particles/neutral_fx/troll_heal.vpcf"
          "EffectAttachType" "follow_origin"
        }
      }
    - Precache block per-ability (particle + soundfile): Each rewritten ability declares its assets in a "precache" KV so the engine loads them when the ability is in play, rather than precaching everything globally in addon_game_mode (whose Precache() is left empty). Note both "particle" (single) and "particles" (folder/extra) keys are used. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\npc\npc_abilities_custom.txt)
      "precache"
      {
        "soundfile" "soundevents/game_sounds_heroes/game_sounds_huskar.vsndevts"
        "particle"  "particles/units/heroes/hero_huskar/huskar_life_break.vpcf"
        "particle"  "particles/units/heroes/hero_huskar/huskar_life_break_cast.vpcf"
        "particles" "particles/status_fx/status_effect_huskar_lifebreak.vpcf"
      }
  SOUNDS:
    - Stateful extend/retract loop with StopSoundEvent pairing: The hook plays a looping 'extend' sound on cast and explicitly StopSoundEvent's it both when a unit is hit (start of retract) and when the hook fully returns; on impact it stops the retract loop and fires a one-shot 'retract_stop'. The looped sound names are passed in as datadriven keys (sound_extend/sound_retract/sound_retract_stop) so the KV owns the soundevent strings. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\heroes\hero_pudge\meat_hook.lua)
    - EmitSoundOn impact at end of motion controller: Huskar's Life Break fires Hero_Huskar.Life_Break.Impact via EmitSoundOn(target) inside OnMotionDone, i.e. the impact sound is gated on the motion-controller actually completing (caster reached target), not on cast — so a cancelled charge produces no impact sound. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\heroes\hero_huskar\life_break.lua)
    - Datadriven FireSound on cast and on tick: Sounds are also fired declaratively: OnSpellStart>FireSound { EffectName "Hero_Huskar.Life_Break" Target CASTER } for the cast, and an item modifier fires n_creep_ForestTrollHighPriest.Heal inside OnIntervalThink. Soundevent .vsndevts files are loaded via the ability precache "soundfile" key. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\npc\npc_abilities_custom.txt)
    - Music status hook (commented scaffold): A SetMusicStatus(DOTA_MUSIC_STATUS_BATTLE, ...) call is present (commented) in OnPlayerUseAbility, showing the intended pattern for forcing the per-player battle-music stinger from a server event. Left disabled in shipping build. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\addon_game_mode.lua)
  GAME-FEEL:
    - Manual 0.03s-tick projectile via SetAbsOrigin (custom motion): Instead of ProjectileManager, the hook is a hand-rolled projectile: a dummy unit is teleported with SetAbsOrigin every 0.03s (Timers loop) along a normalized direction at hook_speed*0.03 units/tick, then on hit/limit the SAME loop reverses to retract. The pulled target is similarly slid back along the line by re-deriving its position from the shrinking distance. This is the game's signature 'feel' — a fully manual, frame-stepped skillshot.
      Timers:CreateTimer(0.03, function()
        dummyLocation = dummyLocation + direction * hookSpeed
        dummy:SetAbsOrigin(dummyLocation)
        distance_traveled = distance_traveled + hookSpeed
        if distance_traveled < travel_distance and not hookTable[caster].bHitUnit then
          return 0.03
        else -- begin retract ...
      end)
    - Parabolic leap via two motion-controller functions (Horizontal+Vertical): Life Break uses ApplyMotionController with separate HorizontalControlFunction (JumpHorizonal) and VerticalControlFunction (JumpVertical). Horizontal slides the caster toward the target each tick; Vertical adds a parabola: it accumulates +velocity/2 to a z offset for the first half of the distance then -velocity/2 for the second half, layered on GetGroundPosition so it arcs correctly over cliffs. Gives a satisfying jump/slam feel.
      if ability.traveled < ability.initial_distance/2 then
        ability.life_break_z = ability.life_break_z + ability.velocity/2
        caster:SetAbsOrigin(caster_loc_ground + Vector(0,0,ability.life_break_z))
      elseif caster_loc.z > caster_loc_ground.z then
        ability.life_break_z = ability.life_break_z - ability.velocity/2
        caster:SetAbsOrigin(caster_loc_ground + Vector(0,0,ability.life_break_z))
      end
    - InterruptMotionControllers as the landing/clamp trigger: Forward motion is clamped two ways: if the caster has crossed max_distance, or once within 100u of the target, it calls caster:InterruptMotionControllers(true), snaps the caster to GetGroundPosition, and only then runs OnMotionDone (damage + impact FX/sound + auto-attack). Tying impact to interrupt guarantees the hit-feedback lands exactly on touchdown.
      if (target_loc - caster_loc):Length2D() > 100 then
        caster:SetAbsOrigin(caster:GetAbsOrigin() + direction * ability.velocity)
      else
        caster:InterruptMotionControllers(true)
        caster:SetAbsOrigin(GetGroundPosition(caster:GetAbsOrigin(), caster))
        OnMotionDone(caster, target, ability)
      end
    - SetForwardVector to snap facing for visual cohesion: On hook hit the target is forcibly turned to face the caster (target:SetForwardVector(direction)) so it visibly gets dragged front-first; the launch dummy is likewise oriented down the cast line. Cheap polish that makes the pull read clearly.
      local direction = (casterLocation - targetLocation):Normalized()
      target:SetForwardVector(direction)
    - Global camera distance override + skillshot accuracy floating notifications: Game-wide feel knobs: SetCameraDistanceOverride(1350) zooms everyone out so global skillshots are aimable; and every hook/arrow cast and connect pushes a styled bottom Notifications line (e.g. live 'hook accuracy 3/7=42.8%') driven by CustomGameEventManager>panorama, which acts as the game's damage/score floating-text feedback layer.
      GameRules:GetGameModeEntity():SetCameraDistanceOverride(1350)
      -- on hook connect (OnEntityHurt):
      Notifications:BottomToAll({text=PudgeHookSuccess.."/"..PudgeHookSum.."="..(math.floor((PudgeHookSuccess/PudgeHookSum)*1000)/10).."%", duration=5.0, continue=true, style={["font-size"]="30px"}})
  AI/COMBAT:
    - Manual skillshot projectile simulation (no ProjectileManager): The combat core is a self-built projectile/aggro-free system: LaunchMeatHook spawns npc_dummy_blank (modifier_phased + a datadriven dummy_modifier), phases it down the cast vector, and uses a per-caster hookTable[caster].bHitUnit flag set by the datadriven OnUnitHit>RunScript(RetractMeatHook) collision to switch from extend to retract. No FindUnitsInRadius / ProjectileManager linear is used — hit detection is delegated to the ability KV's collision, and the script handles travel + retract + the global 20000 range. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\heroes\hero_pudge\meat_hook.lua)
    - Global-range retargeting of vanilla skillshots (the game's whole design): npc_abilities_override.txt rewrites stock abilities (pudge_meat_hook, pudge_dismember, mirana_arrow, shadow_shaman_ether_shock, etc.) with AbilityCastRange 20000 and AbilityBehavior tweaks (e.g. IGNORE_BACKSWING). Combined with the camera zoom-out, this turns normally-targeted/short abilities into map-wide manual skillshots — the 'combat AI' is the human player aiming, scored by the engine. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\npc\npc_abilities_override.txt)
    - Server-side hit/miss accuracy tracking via game events: OnPlayerUseAbility increments per-hero shot counters (PudgeHookSum/MiranaArrowSum) and OnEntityHurt verifies inflictor==pudge_meat_hook/mirana_arrow AND victim:IsRealHero() to count a success — effectively a server-side combat scorer that distinguishes a real hero hit from creep collisions, then broadcasts live accuracy. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\addon_game_mode.lua)
    - Backswing cancel via timed modifier:Destroy(): To make the global hook feel responsive, after a Pudge cast a 0.4s SetThink fires PudgeCancelBackswingThink which finds modifier_followthrough and Destroy()s it, cancelling the attack/cast backswing so the player can act immediately — a manual animation-cancel that improves skillshot cadence. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\addon_game_mode.lua)
    - Auto-attack chaining after Life Break impact: After Huskar's leap completes, OnMotionDone issues a queued DOTA_UNIT_ORDER_ATTACK_TARGET order via ExecuteOrderFromTable, so the hero immediately begins attacking the slammed target — a scripted 'follow-up' that mimics aggressive melee AI behavior for the caster. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\heroes\hero_huskar\life_break.lua)
    - Auto-respawn / mega-creep escalation on team abandon: There is no boss/wave AI, but there is macro 'AI-like' game management: AbandonCheckThink monitors connection state, and when a team empties it sets MegaAutoSpawn, force-destroys that team's barracks (DestroyRadiantBarracks/DestroyDireBarracks via Entities:FindAllByName + ForceKill) to spawn mega creeps and force a finish, plus rebalances gold/respawn coefficients. (C:\Users\work\.dota2-workshop-mcp\reflib\items\466843568\files\scripts\vscripts\addon_game_mode.lua)

## Warchasers (id 300989405)
  PARTICLES:
    - Two-CP beam particle (origin + destination): Healing Wave / Shadow Wave fire a beam by attaching the particle to the caster, then writing source position to CP0 and the target position to CP1. This is the canonical 'lightning/bolt between two units' pattern. Two layered .vpcf (base + _copy) are spawned together for a richer look, and the same CP1 retargeting is reused to bounce the wave from a dummy to the next victim. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/abilities.lua)
      local particle = ParticleManager:CreateParticle("particles/warchasers/dazzle_shadow_wave.vpcf", PATTACH_ABSORIGIN_FOLLOW, hero)
      ParticleManager:SetParticleControl(particle, 0, hero:GetAbsOrigin()) --origin
      ParticleManager:SetParticleControl(particle, 1, target:GetAbsOrigin()) --destination
    - Stored particle handle for explicit lifetime cleanup: Persistent looping effects (tornado_ambient) are NOT released by an event; the index is stashed on the unit (target.tornado) when the effect starts and DestroyParticle is called on that stored handle in the paired End* event. This is the wire-up/clean-up pattern for any aura/channel particle whose lifetime is logically owned by a modifier. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/abilities.lua)
      function TornadoParticle(event)
        target.tornado = ParticleManager:CreateParticle("particles/neutral_fx/tornado_ambient.vpcf", PATTACH_WORLDORIGIN, event.caster)
        ...
      function EndTornadoParticle(event)
        ParticleManager:DestroyParticle(target.tornado,false)
      end
    - Color tint + radius via high control points (CP15/16, CP1 scale): Plagued acid-spray recolors a stock Dota particle to green at runtime by writing RGB Vectors to CP15/CP16, and scales AoE radius into CP1 (Vector(radius/2,1,1)). Lets one borrowed .vpcf serve many affixes/elements without authoring new particles. Crystal Nova similarly drives radius into CP1 as Vector(radius*2,1,radius*2). (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/abilities.lua)
      local p = ParticleManager:CreateParticle("particles/units/heroes/hero_alchemist/alchemist_acid_spray.vpcf", PATTACH_ABSORIGIN_FOLLOW, dummy)
      ParticleManager:SetParticleControl(p, 1, Vector(radius/2,1,1))
      ParticleManager:SetParticleControl(p, 15, Vector(100,255,0)) --color
      ParticleManager:SetParticleControl(p, 16, Vector(100,255,0))
    - Bolt/strike geometry via height-encoded CPs (sky-to-ground): Thunderstorm-style lightning strikes set CP0 to the same XY as the target but Z=1000 (bolt spawn height in sky), CP1 = landing point, CP2 = origin. Spawned in a 1s loop with a fresh FindUnitsInRadius each tick so the strike retargets every beat. Same CP0-is-overhead idiom used for chain_lightning, where CP0/CP1 add GetBoundingMaxs().z to hit unit chest height. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/abilities.lua)
      particle = ParticleManager:CreateParticle("particles/units/heroes/hero_leshrac/leshrac_lightning_bolt.vpcf", PATTACH_WORLDORIGIN, dummy)
      ParticleManager:SetParticleControl(particle, 0, Vector(o.x,o.y,1000)) -- height of the bolt
      ParticleManager:SetParticleControl(particle, 1, dummy:GetAbsOrigin()) -- point landing
      ParticleManager:SetParticleControl(particle, 2, dummy:GetAbsOrigin()) -- point origin
    - Floating damage/heal numbers as msg_fx particles (popups library): Damage/heal/crit/gold/miss numbers are rendered entirely with particles, not panorama. A reusable PopupNumbers() picks particles/msg_fx/msg_<type>.vpcf, attaches ABSORIGIN_FOLLOW, then packs prefix-symbol/number/postfix-symbol into CP1, lifetime+digit-count into CP2, and an RGB color Vector into CP3. Wrapper helpers (PopupDamage red+DROP, PopupCriticalDamage red+LIGHTNING, PopupHealing green+PLUS, PopupGoldGain gold+PLUS) define the game's number vocabulary. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/popups.lua)
      local pfxPath = string.format("particles/msg_fx/msg_%s.vpcf", pfx)
      local pidx = ParticleManager:CreateParticle(pfxPath, PATTACH_ABSORIGIN_FOLLOW, target)
      ParticleManager:SetParticleControl(pidx, 1, Vector(tonumber(presymbol), tonumber(number), tonumber(postsymbol)))
      ParticleManager:SetParticleControl(pidx, 2, Vector(lifetime, digits, 0))
      ParticleManager:SetParticleControl(pidx, 3, color)
    - Bulk precache: explicit list + particle_folder + sync units: Precache() in addon_game_mode does it all up front: ~30 individual PrecacheResource("particle", ...vpcf) calls for borrowed hero effects, a whole-folder grab PrecacheResource("particle_folder", "particles/warchasers") for custom art, PrecacheResource("soundfile", ...vsndevts) per hero soundbank, and PrecacheUnitByNameSync for bosses/summons so spawning them mid-game never hitches. KV abilities additionally carry their own "precache"{ "particle" ... } blocks (e.g. affix_knockback). (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/addon_game_mode.lua)
      PrecacheResource( "particle_folder", "particles/warchasers", context )
      PrecacheResource( "particle", "particles/units/heroes/hero_leshrac/leshrac_lightning_bolt.vpcf", context)
      PrecacheUnitByNameSync("npc_boss", context)
  SOUNDS:
    - EmitGlobalSound for music, stingers and announcer (sounds.lua hub): A central sounds.lua wraps every global cue: door SFX (ui.crafting_slotslide, BARNDOORS_OPEN), discovery/activation stingers (Loot_Drop_Stinger_Short, DOTAMusic_Stinger.007), and per-level battle music (valve_dota_001.music.battle_01..03). Music layering uses laning_01/02/03_layer_01 tracks. BigDoorOpenSound chains two cues via a 0.5/1s Timer for a layered open. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/sounds.lua)
    - Randomized custom announcer lines (Warchasers.* vsndevts): Custom Warcraft-3-style announcer is built from custom soundevents in warchasers_sounds_custom.vsndevts, picked at random: AnnouncerChoose() rolls 1-3 (Warchasers.ChooseHero/ChooseAHero/ChooseWisely), AnnouncerBattle() rolls 1-2, AnnouncerProgress() rolls 1-5 (BravelyDone/Feat/Impressive/PressFurther/TrailOfDestruction). RandomInt avoids repetitive VO. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/sounds.lua)
    - EmitSoundOn vs unit:EmitSound for ability SFX: Ability code uses two forms: unit:EmitSound("Hero_Omniknight.Purification") / caster:EmitSound("Hero_Zuus.StaticField") for an effect tied to a specific unit, and the global helper EmitSoundOn("Hero_Dazzle.Shadow_Wave", target) / EmitSoundOn("Hero_Zuus.ArcLightning.Target", target) for fire-and-forget hit confirms. Borrowed Dota hero soundbanks are precached as .vsndevts. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/abilities.lua)
    - Boss-death audio stinger synced with screen shake: On npc_boss death the kill handler fires EmitGlobalSound("diretide_roshdeath_Stinger") in the same block as a heavy ScreenShake and a camera snap to the corpse, so the audio punch lands with the visual punch. Doom miniboss death plays n_creep_dragonspawnOverseer.Death plus an AnnouncerProgress() line. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/warchasers.lua)
    - Per-hero soundbank precache via PrecacheResource soundfile: Because borrowed Dota hero abilities are reskinned onto custom units, their soundbanks must be precached explicitly: a run of PrecacheResource("soundfile", "soundevents/game_sounds_heroes/game_sounds_<hero>.vsndevts", context) covers dragon_knight, abaddon, sven, lina, zuus, etc., plus stinger banks (game_sounds_stingers_diretide.vsndevts) and the custom warchasers_sounds_custom.vsndevts. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/addon_game_mode.lua)
  GAME-FEEL:
    - Boss-kill screen shake + camera snap to corpse: OnEntityKilled detects the final boss and fires a large ScreenShake centered on the killer, snaps that player's camera onto the dead boss with SetCameraTarget, plays the rosh-death stinger and shows a 'YOU DEFEATED' center message. The big-shake signature ScreenShake(pos, amplitude, frequency, duration, radius, command, bAir) is the game's heavy-hit punctuation; ambient world shakes use a gentler (10,10,7,99999,...).
      ScreenShake(killerEntity:GetAbsOrigin(), 50.0, 50.0, 5.0, 9000, 0, true)
      PlayerResource:SetCameraTarget(killerEntity:GetPlayerOwnerID(), killedUnit)
      EmitGlobalSound("diretide_roshdeath_Stinger")
    - Dynamic camera-distance zoom for set-pieces: Camera 'feel' is driven by SetCameraDistanceOverride on the GameMode entity. Default is 1000; trigger functions push it to 1400/2000 (increase/decrease/high) to pull the camera up for big rooms, and teleport/hint sequences ramp it (1000-1400) to frame cutscene-style moments, then restore 1000. Cheap, no-asset cinematic feel.
      function increase_camera_height_high(trigger)
        GameRules:GetGameModeEntity():SetCameraDistanceOverride( 2000 )
      end
    - Toggleable camera lock (panorama button -> custom event -> Lua): An ARPG-style camera follow: a panorama button (camera_lock_button.js) toggles a bool and sends SendCustomGameEventToServer("player_toggle_camera_lock",{pID,locked}); the Lua handler calls PlayerResource:SetCameraTarget(pID, hero) when locked and SetCameraTarget(pID, nil) to free it. On hero spawn the camera is locked by default via SetCameraTarget(pID, hero).
      if locked == 0 then
        PlayerResource:SetCameraTarget( pID, nil )
      elseif locked == 1 then
        PlayerResource:SetCameraTarget( pID, hero )
      end
    - Knockback + slow on-hit (data-driven motion): The affix_knockback ability gives creeps a 10% on-attack-landed Knockback (Distance 250, Height 50, Duration 0.5, no stun) plus a -50% movespeed slow modifier for 5s, giving melee enemies physical impact feel. Implemented purely in datadriven KV (Knockback action + ApplyModifier), with a Legion duel status FX and wisp tether slow particle for readability.
      "Knockback" { "Target" "TARGET" "Duration" "0.5" "Distance" "250" "Height" "50" "ShouldStun" "0" }
    - Floating combat text vocabulary (colors + symbols): popups.lua defines the readable damage-feedback language: red number with falling-DROP for raw damage, red+LIGHTNING for crits, green+PLUS for heals, gold+PLUS for last-hit gold, purple long-lived popup for %-HP-removal (lifetime 3.0). PopupHPRemovalDamage is wired into void_damage to flash the 400 HP loss from the boss's percent-damage spell.
      function PopupCriticalDamage(target, amount)
        PopupNumbers(target, "crit", Vector(255, 0, 0), 1.0, amount, nil, POPUP_SYMBOL_POST_LIGHTNING)
      end
    - Hitch-free spawning via 1-frame defer + sync precache: To avoid the visible stutter/teleport bug when Lua-spawned units snap to 0,0,0, log_npc defers all post-spawn setup by one frame inside a Timer before reading GetAbsOrigin or attaching think loops. Combined with PrecacheUnitByNameSync for bosses, mid-fight spawns appear smoothly. SetModelScale tweens (ExtraHealth +0.1, growth loops over endTime=i/75) add weight to buffed units.
      -- Wait 1 frame else they'll want to move to 0 0 0 if spawned through lua.
      Timers:CreateTimer(function() ... unit.initial_neutral_position = unit:GetAbsOrigin() ... end)
  AI/COMBAT:
    - Steamtank miniboss: HP-gated waypoint march + ability rotation: Boss 'phases' are encoded as a waypoint table walked by health thresholds: each time healthRemaining drops below the next breakpoint (0.90,0.80,...0.10) the tank advances currentWaypoint and MoveToPosition's to the next hand-placed Vector, toggling immolation at 50%. A SetContextThink('SteamtankThink',...) loop (returns 1s) also re-casts each spell whenever IsFullyCastable: AoE Bomb / Launch Skeletons via FindUnitsInRadius(FIND_FARTHEST hero) + a dummy_unit_spellcast as the cast target, Barrage with no target, and Call Down only below 25% HP (which then EndCooldown's the other spells for a burst). On death it cleans up its npc_roadkill_skeleton adds. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/ai_tank_miniboss.lua)
    - Final boss: EHP-based target selection + 50% HP power phase: final_boss_think scans heroes in 1000 radius (FIND_CLOSEST), then iterates to pick the LOWEST physical-EHP target (physical_ehp = HP * armor mitigation) so the void spell deletes the squishiest hero, casts final_boss_void_new on it and SetForceAttackTarget(target); clears the force-attack when no target. A one-shot phase trigger adds final_boss_buff once HP <= 50% (guarded by HasModifier check so it only fires once). (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/ai.lua)
    - Neutral creep leash / return-to-home think: Every spawned neutral gets a SetContextThink('chase_distance_function',...) that records initial_neutral_position on spawn and, every random 2-6s, MoveToPosition's back home if it has wandered >900 units from its anchor. Returns nil (kills the think) once the unit is no longer a neutral. This is the kiting/de-aggro guard that stops players from pulling the whole map. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/ai.lua)
    - Affix system: random elite abilities scaled to difficulty: affix()/affix_list() turn ordinary creeps into Diablo-style elites: it filters affix_list.txt by the unit's level and melee/ranged type, then AddAbility's DIFFICULTY-many random affixes (knockback, molten, frozen, vampiric, thunderstorm, reflect, illusionist, provoker...) at level 1, caching the rolled set per unit name in affix_table so re-spawns of the same creep keep their affixes. Pre-difficulty creeps are queued and affixed once difficulty is chosen. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/ai.lua)
    - Tank-miniboss reanimation think (datadriven -> Lua): tb_miniboss_think is driven from a KV thinker passing target_entities; it watches its summoned skeletons, and the moment any are dead AND tb_miniboss_animate_dead is off cooldown and not channeling, it CastAbilityNoTarget's animate-dead to resurrect them, keeping the add-swarm topped up. Example of an ability-AI think that reacts to the state of its own summons. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/ai.lua)
    - Projectile patterns: CreateLinearProjectile info table: Linear projectiles are built from an info table (EffectName .vpcf, vSpawnOrigin, fDistance, fStart/EndRadius, iUnitTargetTeam/Type/Flags, fExpireTime, vVelocity = normalized(dir)*speed). MeteorCannon fires one toward the click point; ElectrifiedSparks loops min..max times firing electrified bolts in random directions (RandomInt offset, normalized velocity*300) for a chaotic spray. Aggro/cast targeting elsewhere uses FindUnitsInRadius with FIND_FARTHEST/FIND_CLOSEST to choose victims. (C:/Users/work/.dota2-workshop-mcp/reflib/items/300989405/files/scripts/vscripts/abilities.lua)

## DAWN OF WAR (DDW) - id 1733136384 - Dota 2 auto-battler (Auto Chess style: roll/buy/upgrade heroes on an 8x8 grid, then auto-fights). Three separate utility-scoring AI brains drive every unit. Decompiled at C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files
  PARTICLES:
    - Centralized particle/sound resource registry (res_def.lua) + auto-precache: All reusable VFX live in a global ParticleRes = {} table (TP_START, HERO_REMOVE, SELL_HERO=hand_of_midas, LEVEL_UP=oracle_false_promise, HERO_UPGRADE=hero_levelup, etc). At load, every ParticleRes value is appended into PreloadParticles ('for _,v in pairs(ParticleRes) do table.insert(PreloadParticles,v) end') so the addon-level Precache loops one list. Same pattern mirrored for SoundRes. This decouples gameplay code from raw .vpcf strings. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\res_def.lua)
      ParticleRes.SELL_HERO = "particles/items2_fx/hand_of_midas.vpcf"
      ParticleRes.HERO_UPGRADE = "particles/generic_hero_status/hero_levelup.vpcf"
      for _, v in pairs(ParticleRes) do table.insert(PreloadParticles, v) end
    - Fire-and-forget one-shot: CreateParticle -> SetControl -> immediate ReleaseParticleIndex: For instantaneous effects (hero removed from grid, roll panel open) the index is released on the same frame it is created; the engine keeps the particle alive until it finishes naturally. No timer, no storage. This is the dominant pattern for transient feedback throughout shared.lua/hero_ability.lua. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\shared.lua)
      local p = ParticleManager:CreateParticle(ParticleRes.HERO_REMOVE, PATTACH_WORLDORIGIN, nil)
      ParticleManager:SetParticleControl(p, 0, unit:GetAbsOrigin())
      ParticleManager:ReleaseParticleIndex(p)
      EmitSoundOn(SoundRes.HERO_REMOVE, playerHero)
    - Entity-attached CP via SetParticleControlEnt + attach_hitloc (sell/retrieve item beam): Beam/link effects bind a control point to a moving entity using SetParticleControlEnt(idx, cp, ent, PATTACH_POINT_FOLLOW, "attach_hitloc", origin, false). The Midas sell-hero effect attaches CP1 to the player hero's hitloc so the beam tracks even as units move. Used for retrieve-item, sell-hero, and Rubick-telekinesis hero-move VFX. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\shared.lua)
      local midas_particle = ParticleManager:CreateParticle(ParticleRes.SELL_HERO, PATTACH_ABSORIGIN_FOLLOW, toBeSold)
      ParticleManager:SetParticleControlEnt(midas_particle, 1, playerHero, PATTACH_POINT_FOLLOW, "attach_hitloc", toBeSold:GetAbsOrigin(), false)
      ParticleManager:ReleaseParticleIndex(midas_particle)
    - Multi-particle ability with CUSTOMORIGIN + duration-encoded control points (Glimpse): disruptor_glimpse_new spawns 3 particles (travel + targetend + targetstart), each PATTACH_CUSTOMORIGIN with CP0 bound to the target via SetParticleControlEnt(PATTACH_ABSORIGIN_FOLLOW), CP1 = old location, and CP2 = Vector(dur,dur,dur) to pass the travel duration into the .vpcf. All three are released immediately; the gameplay teleport/damage is deferred via CreateTimer(flDuration). Clean example of packing scalar params into control-point vectors. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\custom_abilities\disruptor_glimpse_new.lua)
      local nFXIndex = ParticleManager:CreateParticle("particles/units/heroes/hero_disruptor/disruptor_glimpse_travel.vpcf", PATTACH_CUSTOMORIGIN, nil)
      ParticleManager:SetParticleControlEnt(nFXIndex, 0, target, PATTACH_ABSORIGIN_FOLLOW, nil, target:GetOrigin(), true)
      ParticleManager:SetParticleControl(nFXIndex, 1, vOldLocation)
      ParticleManager:SetParticleControl(nFXIndex, 2, Vector(flDuration, flDuration, flDuration))
      ParticleManager:ReleaseParticleIndex(nFXIndex)
    - Explicit lifecycle: CreateLogParticle -> deferred DestroyParticle + ReleaseParticleIndex (teleport): Long-lived teleport start/end FX are created with CreateLogParticle, stored in locals, and torn down inside a CreateTimer: DestroyParticle(pfx,false) THEN ReleaseParticleIndex(pfx). This is the correct two-call cleanup (destroy stops emission, release frees the index) used when an effect must be killed at a known moment rather than left to expire. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\shared.lua)
      local start_pfx = CreateLogParticle(tpStartParticle, PATTACH_WORLDORIGIN, nil)
      ParticleManager:SetParticleControl(start_pfx, 0, startPosition)
      CreateTimer(function()
          ParticleManager:DestroyParticle(start_pfx, false)
          ParticleManager:ReleaseParticleIndex(start_pfx)
      end, ...)
    - Modifier-owned particles via self:AddParticle (auto-GC on death/removal) + DOTAScenePanel in UI: Persistent ability VFX are registered to their modifier with modifier:AddParticle(pfx, false, false, 1, false, false) so the engine destroys them when the modifier ends (no manual release). For the shop UI, panorama renders live hero models with $.CreatePanel('DOTAScenePanel', parent, name, {unit:heroName, light:'global_light', particleonly:'false'}) - real 3D models, not images, hittest disabled. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\hero_ability.lua)
      self.target_modifier.tele_pfx = ParticleManager:CreateParticle(ParticleRes.HERO_MOVE_LIFT, PATTACH_CUSTOMORIGIN, caster)
      self.target_modifier:AddParticle(self.target_modifier.tele_pfx, false, false, 1, false, false)
      -- panorama: $.CreatePanel("DOTAScenePanel", heroPic, heroName, {unit:heroName, light:"global_light", particleonly:"false"})
  SOUNDS:
    - Custom soundevents bank (ddw.vsndevts) + named SoundRes registry: Game ships its own soundevents/ddw.vsndevts referenced by short names: DDW.RadVictory, DDW.DireVictory, DDW.BattleDraw, DDW.GameOver, DDW.Victory, DDW.Defeat, plus DDW.KillStreak1..10. Reused stock events are aliased (LEVEL_UP='Hero_Omniknight.GuardianAngel', HERO_REMOVE='Hero_Lion.Hex.Fishstick', COIN_BIG='General.CoinsBig'). PreloadSounds lists all .vsndevts banks (game_sounds, ui_imported, per-hero lion/rubick/omniknight, ddw, chatwheel, chatwheel2023). (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\res_def.lua)
    - Escalating kill-streak global sound with rate-limit (EmitGlobalSound): On each hero kill the attacker's KillStreakInBattle increments; an indexed sound DDW.KillStreak<n> (capped at 10) plays via EmitGlobalSound, but only when streak>=2 AND at least 1s since the last streak sound (LastKillStreakSoundTime guard). Classic announcer-style escalating feedback driving combat hype without per-client events. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\shared.lua)
    - Looping sound start/stop lifecycle: EmitSoundOn(...LOOP) -> StopSoundOn in timer (teleport): Teleport uses a looping channel: EmitSoundOn(SoundRes.TP_START_LOOP, hero) at start, then StopSoundOn(SoundRes.TP_START_LOOP, hero) inside the arrival CreateTimer, followed by positional one-shots EmitSoundOnLocationWithCaster(startPos/targetPos, TP_START/TP_END, hero). Also rate-limited to 3 plays/round via playerInfo.TeleportSoundCount to prevent spam during mass teleports. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\shared.lua)
    - Server-driven client sound bridge (EMIT_SOUND_CLIENT custom event): Server sends a custom game event with {soundName} and panorama plays it locally: OnEmitSoundClient(data){ if(data.soundName) Game.EmitSound(data.soundName); }. Lets server trigger purely-local 2D UI sounds (e.g. per-player feedback) without EmitGlobalSound being heard by everyone. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\panorama\scripts\custom_game\ddw_hud.js)
    - Panorama UI sounds via Game.EmitSound on interactions: Direct Game.EmitSound for click/affordance feedback: 'General.Buy' on opening recycle/roll panel (ddw_hud.js), 'General.NoGold' on a failed purchase (top_layer.js), 'ui.courier_in_use' on skin selection (ddw_elfskin.js). Standard built-in UI sound names, no custom event round-trip. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\panorama\scripts\custom_game\ddw_hud.js)
    - Chat-wheel sound set + per-player anti-spam cooldown: ChatWheelSounds table (CW.Applause, CW.Sad, CW.Cai, CW.ZaiJian, CW.AMS...) backed by chatwheel.vsndevts/chatwheel2023.vsndevts. Each player tracks ChatWheelSoundTime / ChatWheelTextTime in PlayerList to throttle emote spam, and stage transitions emit STAGE_PREPARE='Loot_Drop_Stinger_Legendary' / STAGE_PREFIGHT='GameStart.RadiantAncient'. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\res_def.lua)
  GAME-FEEL:
    - NO ScreenShake / NO hit-stop / NO time-scale (deliberate): Grep across all vscripts found zero ScreenShake, SetTimeScale, or hit-stop usage. As an auto-battler the game intentionally avoids camera-jolt and time-dilation; juice instead comes from economy-feedback layers (overhead gold, escalating sounds, level-up/upgrade particles). Worth noting as a design choice rather than an omission.
      -- (no ScreenShake/SetTimeScale anywhere in scripts/vscripts)
    - Floating combat text via SendOverheadEventMessage (gold rewards): Kill bounties surface as yellow floating numbers over the killed hero using SendOverheadEventMessage(player, OVERHEAD_ALERT_GOLD, killedHero, amount, nil), gated by PlayerResource:IsValidPlayer. Same mechanism reused for buyback/refund. This is the primary 'damage-number'-style floating feedback (gold, not damage).
      PlayerResource:ModifyGold(playerId, goldBounty * 0.8, true, DOTA_ModifyGold_GameTick)
      local player = PlayerResource:GetPlayer(playerId)
      if NotNull(player) and PlayerResource:IsValidPlayer(playerId) then
          SendOverheadEventMessage(player, OVERHEAD_ALERT_GOLD, killedHero, goldBounty, nil)
      end
    - Snap-to-hero camera on round 1 then immediate release: First-round onboarding briefly focuses the camera on the player's hero, then frees it one tick later so control returns to the player: SetCameraTarget(pid, hero) immediately followed by a 0.1s CreateTimer doing SetCameraTarget(pid, nil). CameraInit flag ensures it fires once. The standard 'snap then unlock' camera juice.
      playerInfo.CameraInit = true
      PlayerResource:SetCameraTarget(playerId, playerInfo.Hero)
      CreateTimer(function()
          PlayerResource:SetCameraTarget(playerId, nil)
      end, 0.1)
    - Level-up / upgrade burst particles as progression juice: Reward moments are sold with stock hero-glow VFX mapped in ParticleRes: HERO_UPGRADE='particles/generic_hero_status/hero_levelup.vpcf', LEVEL_UP='oracle_false_promise_cast_enemy.vpcf', GIVE_SHARD=fall_2021 levelup flash. Paired with LEVEL_UP sound (Omniknight Guardian Angel) so every grade-up gives a clear audiovisual pop.
      ParticleRes.HERO_UPGRADE = "particles/generic_hero_status/hero_levelup.vpcf"
      SoundRes.LEVEL_UP = "Hero_Omniknight.GuardianAngel"
    - Stage-transition audio stingers as match-pacing feedback: The match loop (PREPARE / PREFIGHT / FIGHTING / NEWROUND, StageTime={35,4,70,6}) punctuates phase changes with stingers: STAGE_PREPARE='Loot_Drop_Stinger_Legendary', STAGE_PREFIGHT='GameStart.RadiantAncient', and round-end RAD/DIRE_VICTORY/BATTLE_DRAW. The cadence itself (countdown ticks via TIME_TICK='General.CastFail_AbilityInCooldown') is the game-feel.
      GameRules.DW.StageName = {"PREPARE", "PREFIGHT", "FIGHTING", "NEWROUND"}
      GameRules.DW.StageTime = {35, 4, 70, 6}
      SoundRes.STAGE_PREPARE = "Loot_Drop_Stinger_Legendary"
    - Position 'pop' resolve after teleport/spawn (FindClearSpaceForUnit + ResolveNPCPositions): Units are cleanly seated on the ground after moves/spawns: snap Z to GetGroundPosition, then FindClearSpaceForUnit(unit, origin, true) and ResolveNPCPositions(origin, 64) on a 1-frame timer. Prevents overlap/clipping and gives the satisfying instant 'settle' when heroes are placed on the grid or teleported into the arena.
      unit:SetAbsOrigin(Vector(p.x, p.y, GetGroundPosition(p, unit).z))
      FindClearSpaceForUnit(unit, unit:GetAbsOrigin(), true)
      ResolveNPCPositions(unit:GetAbsOrigin(), 64)
  AI/COMBAT:
    - HeroAI - utility-scoring per-hero combat brain (OnHeroThink): The deep combat AI. Each hero runs OnHeroThink on a 0.1s loop; EvaluateCommand scores HERO_CMD_LIST = {ATTACK_TARGET, USE_ABILITY, USE_ITEM, MOVE_TO_POSITION, PICKUP_ITEM, MOVE_ITEM}, highest score wins (ties broken 50/50). Guards for paused game, channeling, command-restricted, reincarnating, and stage timing (won't act in last 3s of FIGHTING). ~5800-line module with ~130 targeting helpers (weakest/strongest/most-lost-HP/by-mana/Rubick-steal, etc). (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\ai_hero.lua)
    - UnitAI - summon/creep AI with behavior-flag-driven spell targeting: Drives summons/illusions (UNIT_CMD_LIST={ATTACK_TARGET, USE_ABILITY}, 0.1s think). GetSpellData inspects the ability's GetBehavior/GetAbilityTargetTeam/GetAbilityTargetType (bitContains DOTA_ABILITY_BEHAVIOR_NO_TARGET/POINT/UNIT_TARGET/DIRECTIONAL/AOE/TREE) to auto-pick a cast mode and target, with a UnitDontCastAbility blacklist (hoodwink_*, tombstone_grab...). Special-cased units: juggernaut_healing_ward heals weakest ally, grimstroke_ink_creature chases nearest hero, brewmaster split kites to fountain. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\ai_unit.lua)
    - BotAI - autobattler economy bot (shop/draft utility scoring): A separate non-combat brain for AI players/takeover. OnBotThink scores BOT_CMD_LIST={MOVE_RANDOMLY, ROLL_HERO, BUY_HERO, UPGRADE_HERO, MOVE_HERO, SELL_HERO, PICKUP_ITEM, GIVE_ITEM}, executes the top one. Uses GetBotHeroLevel(playerLevel) to buy appropriately-leveled units, BotItems table to map each hero to an archetype itemization (MagicItems/TankItems/JumpIntoItems/etc), and force-releases bot control if a human takes over. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\ai_bot.lua)
    - Kiting / retreat via FindGoBackPosition + ExecuteOrderFromTable: Core kiting primitive: HeroAI:FindGoBackPosition(hero, moveLength) computes a retreat point away from enemies (negative length = approach); 20+ call sites issue DOTA_UNIT_ORDER_MOVE_TO_POSITION via ExecuteOrderFromTable. Used to walk out of cyclone/astral-prison, to position before AoE casts (FindGoBackPosition(hero, GetSpellRange)), and the NoNotRetreatHeroes list (axe, pudge, primal_beast, shredder...) opts melee bruisers OUT of retreating. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\ai_hero.lua)
    - Assassin/focus-targeting system (item_assassin_medal driven targeting): A configurable focus-fire layer: GetAssassinTarget / GetAssassinTargetByMainTargetType / IsValidAssassinTarget let players set a target-priority mode (main/secondary target type, ignore-invisible, cast-AoE-on-assassin-target-only) via item_assassin_medal. Even summons honor it - UnitAI checks HeroAI:HasAssassinMedal(owner) and redirects pets onto the owner's assassin target. This is the game's manual-control-meets-AI targeting hook. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\ai_hero.lua)
    - Projectile patterns: CreateTrackingProjectile with ExtraData payload (Luna orbit, Spirit Lance, Glaives): Combat projectiles use ProjectileManager:CreateTrackingProjectile passing damage/stun in ExtraData, resolved in OnProjectileHit_ExtraData(target, location, extradata)->ApplyDamage. Luna's lunar_orbit fans GetAverageTrueAttackDamage-scaled bounces to up to hit_enemies targets via FindUnitsInRadius; PL Spirit Lance fires the main tracking lance + up to 2 extra to nearby enemies on a talent; Silencer glaives apply stacking int-damage on hit. bDodgeable/bReplaceExisting/flExpireTime all set explicitly. (C:\Users\work\.dota2-workshop-mcp\reflib\items\1733136384\files\scripts\vscripts\custom_abilities\luna_lunar_orbit_new.lua)
